# Claude 作業ルール — EQUINOX Lab

> このファイルは Claude Code が本リポジトリで作業する際に必ず守るべき
> ルールを定義する。記事生成・投稿テンプレ作成・データ集計に関する
> ミス防止を最優先する。

## 🚨 鉄則:必ずファクトチェック

ユーザーから 2026-05-13 に「整合性チェック・ファクトチェックを必ず行う」
ように指示があった。以下を**作業終了前に必ず実行**する。

### 記事 (articles/*.html / *.md) を新規作成・編集したら

```bash
python3 scripts/verify_article.py <該当ファイル> [--race "レース名"]
```

**例**:
```bash
python3 scripts/verify_article.py \
  articles/victoria_mile_2026_note_part1.html \
  articles/victoria_mile_2026_promo_part1.md \
  --race "ヴィクトリアマイル"
```

**致命的問題 (❌)** が検出されたら commit せず修正。
**警告 (⚠️)** は内容を確認して必要なら修正。

### 検証ルール

| ルール | 検出方法 |
|---|---|
| ① 年度の連続性 | 2020-2025 中で抜けがあれば ❌ |
| ② 馬名の DB 照合 | 該当馬が `horses` テーブルに存在するか |
| ③ 数値の DB 照合 | 「○○人気・○○倍」が DB の実値と一致 |
| ④ プレースホルダー残存 | `[URL]` `[TODO]` 等が残ってないか |
| ⑤ 矛盾検出 | 同一馬の人気/オッズが文中で不整合 |

## 📝 記事作成時のチェックリスト

新規記事を書いたら以下を確認:

- [ ] 過去レース勝ち馬は**連続した年度すべて**を網羅
  - 例: 2020-2025 を扱うなら 6年分すべて記述
  - 1年でも抜けがあれば検証ツールが ❌
- [ ] 馬名・人気・オッズは DB の数値と一致
- [ ] 「歴代勝ち馬」リストは時系列(年度降順 or 昇順)で統一
- [ ] 表記揺れ無し: 「ヴィクトリアマイル」/「Victoria Mile」 等を混在させない
- [ ] note 用記事は HTML テーブルを使わない (note エディタが潰す)
- [ ] X 投稿テンプレは X:280 文字以内 + Threads:500 以内
- [ ] URL 未確定なら `[URL]` プレースホルダーを残す (テンプレ用)
- [ ] **実投稿(production)では `[URL]` 等のプレースホルダーを残さない**

## 📊 データ集計の鉄則

- 過去6年(2020-2025) を基本範囲とする
- 集計対象が3件未満なら "サンプル不足" と明記
- 単純集計でなく**クロス参照**(出走馬×コース、血統×距離 等)で深掘り
- temporal leakage に注意 (race_date < target_date のフィルタ必須)

## 🤖 投稿テンプレの3要素

すべての投稿に以下を必ず含める:

1. **具体的な馬名**(注目馬・過去勝ち馬の固有名詞)
2. **数値根拠**(なぜそう言えるかデータで示す)
3. **行動指針**(買う/見送る/次の配信告知)

「データ1行 + 結論なし」は最悪パターン (2026-05-13 にユーザー指摘済)。

## 🚨 絶対ルール: 今週レース起点 (2026-06-04 ユーザー指示)

**全ての投稿・記事・データ表示の起点は「今週(対象)のレース」とする。**
それに**出走しない馬・騎乗しない騎手・該当馬がいない血統/パターンの情報は一切出さない**。
過去6年データを使う場合も、必ず「今週の出走馬・騎手・血統と紐付ける」こと。

### 違反例 (二度とやらない)
- ❌ 「鞍上モレイラ 東京芝1600m複勝78.3%」← モレイラは今週騎乗しない (2026-06-04 指摘)
- ❌ 「父Iffraaj 複勝80%」← その産駒が今週出走していなければ表示しない
- ❌ 「リアルスティール×外枠 複勝75%」← 該当する出走馬がいなければ表示しない
- ❌ 「ヤマカツエース 60%(6/10)」← 出走馬の父にいなければ表示しない

### 守るべき設計原則
1. **コース統計を出すなら、必ず今週の出走馬/騎手と交差**して該当者のみ表示
2. **lede ヘルパー** は `race_id` を受けて出走馬・騎手の集合と AND を取る
3. **sec_* 関数** は「該当馬なし」を出さない (集計対象から除外)
4. **過去傾向(例: 1人気の (1-2-2-1) など着度数)** は今週の出走馬と直接結びつかなくても OK
   ← ただし「今週この傾向どう動くか」の示唆を添える
5. データ取得時点で出走馬未確定なら lede 自体を出さない (誤誘導禁止)

### 実装チェックポイント
- 騎手系セクション/lede: `WHERE jockey IN (今週の出走馬の騎手)` を必ず適用
- 血統系セクション/lede: 「該当馬: ◯◯」が空なら表示しない、TOP も除外
- 枠順バイアス: 枠順抽選後のみ、出走馬の枠順を表示
- 上がり最速の傾向: コース全体の話なので例外的に OK

## 🔧 投稿スケジュール (平日)

JRA の出走スケジュール:
- **月**: 特別登録
- **木**: 出走馬確定 + 斤量 + 馬番
- **金 11:00**: 枠順抽選
- **土・日**: レース当日

これを踏まえた slot 配置:

| 曜日 | 朝 7:30 | 昼 12:30 | 夜 20:00 |
|---|---|---|---|
| 月 | 週末ラインナップ | レース#1コース傾向 | レース#1 AI注目TOP3 |
| 火 | レース#2血統 | レース#2 AI注目TOP3 | レース#2 詳細 |
| 水 | 🏇 騎手×コース | 追い切り情報 | 危険な人気馬 |
| 木 | 出走確定+血統 | 8軸最終TOP4 | 最終3頭+note |
| 金 | 🔮 AI独自パターン分析 | 🎯 注目馬3頭と根拠のみ | 🔔 翌朝配信告知 |

「金朝に枠順発表」「金昼に買い目フル公開」は **NG**
(買い目は土朝に集約)。**枠順の発表タイミングはレースで異なる: 2026宝塚記念は木曜16時台に確定済みを実測** (#2 の「金11時抽選」は全レース共通ではない)。馬番ガードは DB の post_position 駆動なので、発表日がいつでも「DB に入ったら解禁」で自動追従する。

## 💻 開発時のルール

- DB の正本は git 上では **keiba.db.gz** (#64: raw が GitHub 100MB 制限超過)。作業ファイル keiba.db は untracked。**pull 後は `bash scripts/db_unpack.sh`、commit 前は `bash scripts/db_pack.sh`**。write の取り消しは db_unpack.sh 再実行 (= **DB 補完作業中は unpack 禁止。書き込み完了まで pull/unpack しない**)
- features.py と fast_train.py の `get_feature_columns()` は**完全一致**
- 投稿履歴: `docs/data/.post_history.json` が git tracked / source of truth。ローカルの `.post_history.json` は .gitignore 対象のフォールバックバックアップ
- weekly_retrain が走ると models/*.pkl が更新される(自動デプロイ)
- ML 特徴量を変えたら **必ず retrain が必要** (古い models/*.pkl は新 features で predict できずエラー)
- confidence.py は **v4 で ROI 期待値ベース** に再設計 (2026-05-25)。trio_ev/odds_pot/umaren_ev/top3/conc の 5軸。旧 NORMS は廃止
- 結果スレッド投稿の重複検出は **「集計ツイート」(`集計` + `◎の戦績`/`印馬の` を含む)** で判定 (PR #88)。tweets[0] は固定 header / tweets[1] は別レース構成で誤判定リスクあり、フォールバックは廃止 (2026-05-25)
- 血統データは horses テーブル全体で 60%+ 欠落しがち → `scripts/auto_pedigree.py` (cron で自動補完) を整備済

## 🐛 過去のミス事例 (繰り返さない)

### 🚨 教訓 TOP5 (これ守れば類似事故 90% 防げる)

1. **予測ロジック変更時は seal を NULL に戻して再 predict** (#12, #17 関連)
   ```sql
   UPDATE predictions_cache SET posted_at = NULL
   WHERE race_id IN (SELECT race_id FROM races WHERE race_date='YYYY-MM-DD');
   ```
2. **GitHub Actions では cache restore 直後に git の DB を強制復元** (#19)
   ```yaml
   - name: Force git DB (override stale cache)
     run: git checkout HEAD -- keiba.db
   ```
3. **X 投稿コマンドには時間ガード必須**(post_predict / odds_flash etc, #15)
4. **データパイプライン変更時は preflight_check.py で完整性確認** (#13, #14, #18)
   ```bash
   python3 scripts/preflight_check.py YYYYMMDD --auto-fix
   ```
5. **結果系投稿は「投稿対象レース全完走」が条件** (post_predict 時の seal を完走確認の trigger に)

---

### 📊 データパイプライン関連 (収集 / 同期 / cache)

- **#11 (2026-05-16)**: 血統補完中に `git checkout -- keiba.db` で補完分消失 → **DB 書込中は git pull/checkout 禁止**
- **#13 (2026-05-23)**: `fetch_weekend_races` cron が月水金朝のみで土日朝発火せず → R1-R8 欠落で結果反映停止 → **土日朝7時(UTC 22時)発火 cron に統合** (PR #92)
- **#14 (2026-05-23)**: `scrape_shutuba` は `entries` キー、`save_race_to_db` は `results` キーで6ヶ月以上未来レース出走馬保存されず → **両キー受付に修正**
- **#18 (2026-05-24)**: パイプライン全段階で完整性チェック無く変な出力が垂れ流し → **`scripts/preflight_check.py` 新設** (races件数 / 出走馬 / 予測キャッシュを auto-fix)
- **#19 (2026-05-24)**: 各 workflow が `actions/checkout` 直後に `actions/cache restore` で keiba.db を **cache 版で強制上書き**。git push した正しい DB が反映されず古い予測 (◎エンネ) が UI に → **全 9 workflow に `git checkout HEAD -- keiba.db` step 追加**

### 🤖 ML / 予測ロジック関連

- **#4 (2026-05-09)**: `combo_top3` で temporal leakage の偽 importance → **v6 で時系列累積化**
- **#6 (2026-05-16)**: ML から popularity 完全削除 → バックテスト ROI -7pt → **当日 revert** (PR #67)。教訓: 「市場の集合知を捨てるな」
- **#10 (2026-05-16)**: 信頼度 S < A の逆転 (S:27.8% / A:35.3%) → AI 過信・少頭数バイアス・クラス偏り → **WEIGHTS で pop_score 0.30、post-calibrate 12%超過60%圧縮** (PR #64)
- **#12 (2026-05-16)**: 予測ロジック変更しても DB の seal で古いまま → 明日朝 cron で古い予想 (◎17人気) 投稿寸前。dry-run で発見 → **「予測ロジック変更 = seal NULL + 再 predict 必須」**
- **#16 (2026-05-23)**: `should_bet_race` の閾値 30% が post_calibrate v8 後の分布に追従せず買い目0点 → **閾値緩和** (30%→23%, top_prob 10%→8%)
- **#17 (2026-05-24)**: Contrarian 補正が ML 識別不能レースで暴走、18人気馬が ◎ に → **ML 勝率レンジで強度可変化** (レンジ<3%停止、<5%で30%、それ以外100%)
- **#20 (2026-05-24)**: **旧 印 × 注 が「機能不全」(単独 ROI 50-69%)** → 3-5月 7,000R バックテストで判明、旧 × 注 ロジックは単独 ROI が市場標準を下回りノイズ印化していた。**穴馬スコア(SI/人気 + コース適性 + 血統 + 穴予兆 + 騎手)を新設して × 注 のみ置換** (改修済み、現在は機能良好): 単独単勝ROI ×50%→95% / 注69%→136%、 **◎軸三連複ROI 130%→174%** に劇的改善。◎○▲△ は現状ロジック維持(◎軸三連複の構造強度を保つため)。
- **#21 (2026-05-25)**: v4 信頼度 (ROI 期待値ベース) 投入後の整合性監査で **致命的不整合 5件 + 警告 7件** を発見。修正済み: (1) `evaluate_from_horses()` に `odds_key` 追加 / (2) `generate_note.py` 非 cached path を v4 化 / (3) `app.py` ダッシュボードを v4 化 / (4) `cmd_predict` を `should_bet=1 AND confidence in (S,A)` 化 / (5) `cmd_morning`/`cmd_weekday`/`cmd_evening` に時間ガード追加 / (6) tweets[1] フォールバック削除 (旧バグ復活リスク) / (7) preflight_check を manual_predict/race_day_runner にも組込 / (8) seed_historical.yml cache key を run_number に統一。**教訓: 大規模ロジック変更時は呼び出し元全箇所を grep して影響範囲を網羅すること**。

### 📝 記事 / X 投稿関連

- **#1 (2026-05-12)**: 「中身なし投稿」放置 → **v8 抜本リデザイン** (PR #40-44)
- **#2 (2026-05-13)**: 「金曜朝=枠順発表」と誤定義(実際は11時抽選) → **金朝はAI独自パターン分析に変更**
- **#3 (2026-05-13)**: ヴィクトリアマイル 2023 ソングラン抜け → **`verify_article.py` 新設**(年度連続性チェック)
- **#5 (2026-05-10)**: 「[7-1-1]」を「連勝中」と誤読(競馬慣習は着度数) → **「7着→1着→1着」表記に統一**
- **#7 (2026-05-16)**: 血統データ 66%欠落で 8軸スコア過小評価 (◎エンブロイダリー漏れ) → **`scripts/auto_pedigree.py` + cron 自動補完**
- **#8 (2026-05-16)**: post_predict の印表記「◎{馬名}」で馬番なし fact_check ブロック → **「◎ {番}番 {馬名}」形式統一** (PR #60)
- **#9 (2026-05-16)**: 結果スレッドの重複検出 tweets[0] 先頭一致で誤判定 (続編永遠に投稿不能) → **tweets[1] (2件目) で判定** (PR #69)、その後 results は集計ツイート判定に変更 (PR #88)
- **#15 (2026-05-23 16:33)**: workflow_dispatch (GAS 等外部)で post_predict 誤発火、レース真っ最中に予想投稿 → **`cmd_predict` に時間ガード追加** (11時以降スキップ)、後に odds_flash 等にも展開
- **#22 (2026-05-25)**: GitHub Actions cron が**1-4.5時間遅延発火**することが頻発 (5/25 朝 cron 7:30→8:32 = 1h遅、昼 cron 12:30→17:03 = 4.5h遅)。狭い時間ガード (例: 11:00-14:59) だと正常な遅延発火まで skip してしまう → **時間ガードを各 slot +1h 拡大** (cmd_morning 7-11 / cmd_weekday 11-15 / cmd_evening 19-23)。ただし 4.5h 遅延級は依然 skip (誤投稿事故防止優先)。教訓: 「cron は ベストエフォート、時間ガード窓は cron 遅延分も含めて設計」。
- **#23 (2026-05-25)**: X API `403 Forbidden` 発生 (5/25 朝 cmd_morning)。`get_users_tweets` (read) と `create_tweet` (write) が両方 403 = 一過性ではなく認証層/app tier の問題と推測。post_x.py の error logging が貧弱で原因切り分け不能だった → **詳細ロギング強化**: `e.response.text` / `e.api_errors` / `e.api_codes` を抽出して原因 (token 失効 / tier 制限 / 重複 / アカウント制限) のヒントを出力。Threads は同時刻に成功 = X 側固有の問題。**確認手順: https://developer.x.com/en/portal/dashboard で tier と直近 errors を確認**。
- **#24 (2026-05-26)**: 目黒記念2026 記事で「1番人気が3勝/4」と本文と矛盾した数字を書いた (実際は1人気は2勝、過去4年は1人気2勝+2人気1勝+4人気1勝)。**毎回同じパターンのミス (馬番抽選前表示、「印」確定前使用、内部メッセージ垂れ流し、数値矛盾) を指摘されて直す悪循環** → 「学習する機能はないのか」とユーザー指摘。**システム化** で対処: `scripts/verify_article.py` に `check_numeric_claim_consistency()` 新設。「N番人気がX勝」クレームを本文中の年度別人気列挙と自動カウント照合、不一致なら🚫ブロック。同様パターン (馬番抽選前、印確定前) も将来 verify_article.py に追加すべき。**教訓: 人間の検算 → 機械的検算へ。「気をつける」では再発するので、コードレベルで違反を不可能にする**。

### 💰 戦略 / ROI 最大化

- **#25 (2026-05-26) — W1 ROI最大化施策**: 7層 MECE 監査の結果、現状コードは confidence=C/D でも `should_bet=1` で投資されており、5/9-5/25 backtest で C 三連複◎軸 ROI 46%、D 31% (損失層) と確認。Δ ROI = +22.3pt 改善余地。**対策**: (1) `strategy/betting.py:should_bet_race` に `confidence` パラメータを追加して C/D は明示的に return False、(2) ◎オッズ妙味バンド検査 (2.0倍未満は配当妙味なし / 15倍超は◎信頼度低い大穴) を追加、(3) `predict.py:cmd_predict` で confidence 計算後に should_bet を再評価 (旧 path では confidence 不明状態で先に should_bet 判定していたため C/D も bet=1 のままだった)。バックテスト: 全体 ROI 69.1% → S+A のみ ROI 91.4% (Δ+22.3pt)。**教訓: confidence は予測の補助指標でなく投資判断の主軸。「分かるレースだけ買う」がROIの本質。出走馬血統データの完全 backfill (5/31 ダービー20頭 + 目黒16頭) も同時実施**。
- **#26 (2026-05-26) — 単日支配バイアス発見**: #25 の検証中に発見。**5/9-5/25 の S+A 馬連 ROI 151% は、5/9 単日 (ROI 339%) の lucky day に支配されていた**。5/9 除外で ROI 80% に転落 (損失層)。三連複も 91% → 65%、ワイドも 93% → 67% と同様。「短期間 backtest の絶対値 ROI は信用できない」という普遍的教訓。対策: race-data-analyst skill に **「単日支配チェック」を必須化** — 単日 spend > 20% / 除外時 ROI ±20pt 以上ぶれたら警告フラグ。2021 backfill 完了後にサンプル拡大して再評価必要。**教訓: ROI 報告には必ず日別 spend/return の表を添付。「優位性 +22pt」と書くなら「単日除外で +X pt」も併記**。
- **#27 (2026-05-27) — W2 confidence-aware bet weighting + 機械化セーフガード 3 本**: W1 で C/D 遮断・odds band 追加した上で、さらに「高信頼レースに重く賭ける」設計を導入。`strategy/betting.py:CONFIDENCE_MULTIPLIER` で `S=2.0x` (1点200円/budget2000円) / `A=1.5x` (line数拡大) / `B=1.0x` / `C/D=0.0` (二重防御)。同日に **3 つの機械化セーフガード**を追加: (1) `verify_article.py:check_horse_numbers_confirmed` — JRA金11時抽選前に記事で馬番を書くと 🚫 ブロック (post_position NULL/0 を確定シグナルに採用、出馬表時点の horse_number=出走順ではなく)、(2) `verify_article.py:check_marks_confirmed` — predictions_cache 未生成時に印 (◎○▲) 使用なら ⚠️、(3) `scripts/roi_weekly_monitor.py` — 週次 ROI 集計 + 4 週 rolling + 異常週 (ROI>200% or <30%) 自動 flag、JSON 出力 (`docs/data/roi_weekly.json`)。初回実行で 5/11 週 ROI 29.8% を「異常低」検出。**教訓: #24 の延長 — 人間の指摘 → 機械的ブロック化を徹底。「気をつける」では再発するので、verify_article で commit 前に止まる仕組みを増やす**。同時に 2021 historical races (3,456 R) を DB 取り込み済 = 6 年データ揃った。
- **#28 (2026-05-27) — 未勝利・新馬戦は confidence の予測力が逆転**: 5/9-5/25 race_class × confidence × 三連複◎軸 ROI cross-tab で発見。未勝利・新馬層: S=39% / A=38% / B=55% / C=64% / D=77% (高 confidence ほど低 ROI = **完全逆転**)。一方 1勝クラスは A=189% で正常、2勝クラスも A=234%。**ML が unknown horse を学習できておらず、未勝利戦に対する confidence 評価が無効**。対策: `volatility.py:compute_race_volatility` に race_class 検出を追加、race_name に「未勝利」「新馬」が含まれたら `conf_adjust = -2` で強制 2 段下げ (S→B / A→C / B→D)。should_bet_race の C/D 遮断と組み合わせて未勝利戦は実質投資対象外に。バックテスト: 全 confidence 投資 ROI 69.1% → 未勝利 -2 補正後 **113.5%** (Δ+44.4pt、初の 100% 超え)。spend は 60% 削減、return は 43% 削減 = 未勝利投資の純損失を遮断したと解釈。**教訓: 「ML の confidence は学習データ範囲内でのみ有効」。新馬・未勝利は経験データそのものが少ないので、ML の高信頼判定そのものが noise**。同日 dry-run で発見した穴予兆 NoneType crash (jockey_name=None) も修正。
- **#29 (2026-05-27) — 単年 historical backtest で 5/9 単日支配の影響が判明**: `scripts/historical_backtest.py` で 2025 単年 (3,451 races) 再現。**全体 ROI=48.2% (損失層)** — 5/9-5/25 backtest が示した 113% は 5/9 単日支配 (lucky day) の過大評価だった事実が確定。ただし confidence 軸の **相対順位は維持** (S=57.9% > B=47.3% > A=31.0%)。投資レース 764/3451 (22.1%)、スキップ 43.8% (主に未勝利 1554 件 → 補正で D に落として除外、正常動作)。**A 層が S/B より低 ROI なのは要調査** (特に 1勝 × A=23.9%、OP/特別 × A=33.7%)。**教訓 1**: 短期 backtest (17日) の絶対値 ROI は信用できない。年単位での検証が必要。**教訓 2**: confidence の relative order は valid だが、絶対 ROI を 100% 超えるには更なる loss layer 識別が必要。**教訓 3**: A 層の質改善が次の最大効果見込み (A 層 + B 層は volume が大きく寄与大)。結果ログは `docs/analysis/historical_backtest_2025_*.log` 参照。
- **#30 (2026-05-27) — 1勝クラス × loss layer 4本補正**: `scripts/analyze_a_tier_loss.py` で 1勝×A の 128 races (ROI 24.9%) を分解、loss を構成する 4 パターンを特定。(1) ダート 1勝戦 ROI 15.5% (n=77) — 芝 39.0% に対して大幅低、(2) 阪神 5.2%/中京 11.9%/札幌 0% — 中央場 (東京 43.9%/中山 49.6%) との明確な格差、(3) 短距離 (~1400m) 11.4% — 長距離 79.3% との対比、(4) ◎ 2.5-3.9倍 11.5% — 1.x-2.4倍 28.3% より低い妙味中間帯。**対策**: (a) `volatility.compute_race_volatility` に 1勝クラス検出を追加、ダート/ローカル/短距離 各 -1 で最大 -2 に capped (S→C, A→C で should_bet=False)、(b) `should_bet_race` に `race_info` パラメータ追加、1勝戦×◎2.5-3.9倍は明示的見送り。**設計判断**: 2勝以上/OP/特別/G1-G3 は ML が機能してるので適用外 (cross-tab で確認済)。1 つの signal でも -1 (A→B で bet 額減)、2 つ以上で -2 (skip)。**教訓: 「同じ confidence でも race_class × surface × venue × distance で ROI が劇的に違う」。confidence 軸単独では限界、cross-tab 分析が必須**。◎着順分布で「◎は 70% で 3着内」だが「相手不足 22.7%」も発見、これは将来の相手選定強化課題。
- **#31 (2026-05-27) — Walk-forward で 100%+ 安定 segment は 2 つだけ**: `scripts/walk_forward_segment.py` で 6 年 (20,009 races) 検証。**全 6 年で ROI ≥ 100% を維持できる segment は ZERO**。5/6 年安定は **2 segments のみ**: (1) 単勝 × confidence=S × 2勝クラス × 中距離 (avg 117.5%、min 54.7%)、(2) 単勝 × 函館 × ダート (avg 106.5%、min 61.0%)。両 segments とも 2023 が大穴年 (55%, 61%)。年間 ~74 races / 利益 ~1,000円。**教訓: 短期 backtest の 100%+ は ほぼ全部 lucky day or 期間バイアス。長期検証で生存する segment は極めて限定的**。
- **#32 (2026-05-27) — Phase α 完成 (100% 超え路線の 4 本柱)**: walk-forward で 100% 超え segment は限定的と判明 → プロ馬券師の手法を反映した 4 改修を実装: (1) **EV>1.2 厳格化**: 全 6 券種で `MIN_EV (1.2)` 統一 (旧 0.3-1.0)。honor_bets も EV >= 1.2 強制。(2) **Whitelist specialization**: `WHITELIST_SEGMENTS` クラス変数で walk-forward 確認の 2 segments のみ通過、`USE_WHITELIST=1` env var で起動可。(3) **馬体重変化補正** (`predict.py`): DB の weight_change を post-prediction で乗算補正 (±2: 1.0 / ±3-5: 0.97 / ±6-9: 0.90 / ±10+: 0.80)。(4) **馬場バイアス検出** (`scripts/track_bias_detector.py`): 同日 R1-R8 の post_position 別 3 着内率を集計、R9+ に内枠/外枠 ±8% 補正。実テスト: 2025-04-13 阪神 outside bias 検出済 (内 29% vs 外 58%)。**Phase α 効果計測 (2025 全 3,434 races)**: 単勝◎ ROI = baseline 96.9% / alpha 93.0% / **WHITELIST mode 113.4%** 🟢 (n=83)。**初の真の 100% 超え達成**。三連複は WHITELIST mode でも 30.2% で論外 → **単勝に絞るのが最適**。**教訓: 体重補正・馬場補正の全 race 適用は逆効果。specialization (whitelist × 単勝) が唯一の黒字路線**。
- **#33 (2026-05-27) — cmd_morning/evening が thread (list) 受け取れず TypeError**: 5/26 deploy の `805fc55` で slot_composer が `list[str]` (thread) を返すように、`b786033` で post_x.py から slot_composer を呼ぶ動線に切替。しかし `cmd_morning` / `cmd_evening` の `fact_check_tweet(tweet)` は `str` 前提のままだったため、5/27 朝の morning cron が `TypeError: expected string or bytes-like object, got 'list'` で失敗 (post_x.py L3425)。**修正**: `fact_check_tweet_or_thread(tweet_or_thread)` helper を新設、`str` → 旧関数に委譲 / `list` → 各要素を check。cmd_morning / cmd_evening を helper 経由に。**教訓: 戻り値の型を変える deploy は呼び出し元の grep が必須**。slot_composer 動線切替時に str→list 互換性を確認していなかった (system-integrity-audit skill のチェック対象になるべきパターン)。
- **#34 (2026-05-27) — 投稿 SLO 保証システム (cron 遅延・失敗の構造的解決)**: #22 (cron 1-4.5h 遅延) + #33 (TypeError) で 「ユーザーが気づくまで投稿されない」状態が続いていた。「夜の投稿なんでされないの? 解約しようかな…」のユーザー指摘で **「基本ルールを守る仕組み」を構造的に欠いている**ことが判明。**対策**: 4 コンポーネントの SLO 保証システム新設。(1) `config/posting_slo.yml` で 8 slot の expected/deadline/fallback_mode を declarative に明示、(2) `scripts/check_posting_health.py` で各 slot の status (ok/ok_late/missing/missed/future/not_today) を判定、(3) `.github/workflows/posting_watchdog.yml` を 30分 cron で動かして missing を自動 trigger、(4) `docs/data/posting_health.json` で常時可視化。**期待効果**: 今回のような事故は 30分以内に自動検知 + 自動 fallback で復旧。**教訓: 「投稿しない側」の守り (時間ガード) はあったが「投稿する側」の保証が無かった = 不完全な SLO。ルールは config に declarative に書き、code でなく仕組みで強制する**。
- **#36 (2026-06-06) — 土日朝のダッシュボード404再発 (deadline=08:30が遅すぎ + GitHub Actions cron スキップ)**: ユーザー「予想の取得に失敗: 20260606のデータがありません（404）、先週もそうだった、根本解決して二度と起こさないようにして」。調査で **6/5 22:00 UTC の土朝 predict cron が GitHub Actions のスケジューラ遅延・スキップで未発火**だったと判明 (run history に該当 timestamp なし)。DB に出走馬 332頭は揃っていた (fetch_weekend_races は 06-05 21:18 success) が、AI予測 (predictions_cache) は 0件 → JSON 未生成 → GitHub Pages 404。SLO watchdog (#30) は走っていた (06-05 21:53 UTC = 6/6 6:53 JST、06-05 22:57 UTC = 6/6 7:57 JST) が、`weekend_morning_predict` slot の `watchdog_deadline_jst: "08:30"` まで「未到達=future」と判定し自動補完を発火しなかった。**緊急対応**: ローカルで 6/6+6/7 両日の refresh_entries → refresh_odds → predict → export を手動実行し JSON を push (75a1cb5)。**根本対策**: `config/posting_slo.yml` の `weekend_morning_predict.watchdog_deadline_jst` を `08:30` → `07:30` に短縮。これで cron 7:00 が遅延しても 7:30 watchdog run が deadline 到達と判定し fallback auto を即 trigger する。ユーザーが 7時台にダッシュボードを見ても予測反映済の状態になる。**教訓: GitHub Actions cron は遅延だけでなく完全スキップもある (今回 6/5 22:00 UTC の run が存在しない)。SLO deadline は「expected + 30分」程度に絞り、watchdog 30分間隔と組み合わせて cron 遅延 1tick 後には必ず補完する設計に。「fetch (data) は来てるのに predict (生成) だけ動いてない」パターンは #35 と同型の構造的弱点 = 段階的に「データ取得 → 生成 → 投稿」の各段で別 watchdog が必要**。
- **#35 (2026-05-30) — weekend predict が scrape で stuck + 監査で post_predict 取りこぼし発見**: 5/30(土) 朝「予想システムに今日のデータが入っていない」とユーザー指摘。調査の結果 (1) **fetch (出走馬) は 05:00 に完了済 (361頭)**、(2) **07:00 の predict cron が `predict.py collect` (全レース再 scrape) で 15分+ stuck** し予測が生成されていなかった。真因は「fetch_weekend_races が既に出走馬を入れているのに predict step が無条件で再 scrape」。**対策1**: predict step で既存出走馬 ≥50頭なら collect skip + `timeout 300` でラップ (auto_post_x.yml)。**対策2 (監査由来)**: Explore で全モジュール監査 → `post_predict` のレース選定が `should_bet==1 AND confidence in (S,A)` で、大頭数レースは top_prob<12% → should_bet=0 になり信頼度 A レースを取りこぼす問題を発見。should_bet 必須を撤廃し confidence S/A 基準に変更 (post_predict は投資推奨でなくコンテンツなので)。fetch/predict/post の全フロー watchdog 監視下 + missed なら GitHub Issue 通知 (secret 不要)。**監査の誤検知も記録**: volatility `idx-adj` 符号 (adj=-2 → idx+2 = 正しく降格)、pred_top3 再正規化漏れ (複勝率は合計~3.0 が正常、1.0 正規化は逆にバグ) は両方 false positive と確認。**教訓: データパイプラインは段階ごとに「いつ入る想定か」を明示 (fetch 05:00 / predict 07:00 / post 10:15)。前段が済んだ処理を後段が重複実行して stuck する設計を避ける。should_bet (投資判定) と post 選定 (コンテンツ) を混同しない**。
- **#64 (2026-06-10) — keiba.db が GitHub 100MB 制限超過 → gz 圧縮を git 正本に移行**: 宝塚記念記事用の 2024 backfill (seed_historical) が **push 時に「File keiba.db is 101.81 MB; exceeds 100.00 MB」で silent failure** していた。調査時点で DB は 98.82MB = 残り 1.18MB であり、次の週末収集で全 workflow の DB push が確実に死ぬ状態だった (発見が土曜なら週末データ全損)。**対策**: git の正本を `keiba.db.gz` (gzip -9n, 28.7MB = 3.4x 余裕) に移行。(1) `scripts/db_pack.sh` (`-n` で timestamp 除去 = 内容同一ならバイト同一で no-op commit 防止、pack 前に wal_checkpoint) / `scripts/db_unpack.sh` 新設、(2) 全 10 workflow を機械置換 — Force-git 復元は `checkout -- keiba.db.gz + db_unpack.sh`、commit 側は `db_pack.sh + git add keiba.db.gz`、weekday_runner の lock 読みも gz 経由、seed_historical の `--theirs` 解決も gz に、(3) keiba.db を .gitignore + git rm --cached (作業ファイルは untracked = WAL flush で git が dirty になる #52 問題も構造的に消滅)。副産物: 2024 宝塚記念は当日分を直接 scrape して即日反映 (京都開催・ブローザホーン)。**教訓 1: 「silent failure する外部制限 (file size / quota / rate limit)」は使用率を監視しないと、ある日突然全系統が止まる。教訓 2: backfill 等の一括 job が落ちたら、エラーの 1 行目だけでなく push 段の log まで読む (今回 scrape は成功していて push だけ死んでいた)**。
- **#65 (2026-06-10) — 通過順 (passing_order) が 2021-2023 でほぼ全滅 → アーカイブページから backfill**: 宝塚記念記事の検証中に発覚。`scrape_race_result` は race.netkeiba.com の result.html を参照するが、**過去レースのこのページには「通過」列が存在せず**、旧 seed で取り込んだ 2021 (92%欠落)/2022 (100%)/2023 (100%) の通過順が空だった (2020/2025 はライブ収集経路のため健在)。一方 **db.netkeiba.com/race/<id>/ のアーカイブページには通過・上りが常設**で、既存の `_build_col_map` (ヘッダー名ベース) でそのまま取れる。**対策**: (1) `scraper.scrape_race_result_archive()` 新設、(2) `scripts/backfill_passing_order.py` + `backfill_passing.yml` (dispatch) で年単位一括補完、(3) ついでに premium 列「上がり指数」が `"上がり" in h` で last_3f を乗っ取るバグも修正 (`"指数" not in h` ガード)。宝塚記念 4 年分は即時補完し、6年完全の通過順で記事の「阪神道悪=4角先頭」主張をデータ確定させた (良馬場は4角2-9番手と幅、9番手差し切りはイクイノックスのみ)。**教訓: 「データが無い」と「取り損ねている」は別物。欠落率が年で不連続 (92-100% vs 0%) なら、それはデータソースの問題ではなく収集経路の差異を疑う**。
- **#66 (2026-06-11) — 「前走」クレームに日付確認を怠り 13ヶ月休み明けを今季の前走と誤記**: 宝塚記念後編で「⑥ビザンチンドリーム 前走 天皇賞(春)2着 (2024年覇者と同じ前走内容)」と書いたが、その春天2着は **2025年5月 (13ヶ月前)** のもので、2026年春天2着は別馬 (ヴェルテンベルク)。「直近走を ORDER BY date DESC LIMIT 1 で取る」だけだと長期休養馬を今季ローテと誤読する。ユーザー指摘で発覚し、王道ローテ該当を4頭→3頭に訂正。**教訓: 「前走」を語るときは必ずレース日付を併記・確認する (休み明け日数はそれ自体が重要ファクター)。記事の「同じ前走内容」のような類推クレームは、レース名+着順だけでなく時系列文脈まで一致して初めて成立する**。
- **#67 (2026-06-13) — 収集系 (race_day_runner) の cron + 専用 watchdog が同朝全滅、結果反映が停止**: 土曜朝、predict は GAS 第3層が 7:49 に回収して配信成功した一方、**race_day_runner の 5 crons (8:30-10:00) と refresh_watchdog の全 crons (8:45〜) が一本も発火せず**、結果収集が始まらないままユーザー指摘 (10:26) で発覚 → 手動 dispatch で即復旧。**真因は構造**: 投稿系は GH cron/runner/GAS/watchdog の4層 (故障モード独立) なのに、収集系は「runner 自身の cron + refresh_watchdog」の **2層ともGitHub スケジューラ依存 = 相関故障で一蓮托生** だった。同朝 posting_watchdog (*/30) は生存していた実績から、**posting_watchdog に「土日 8:40-17:00 に runner 不在なら再 dispatch」step を統合** (収集系の第3層)。**教訓: 冗長化は「層の数」でなく「故障モードの独立性」。同じスケジューラに乗った watchdog は watchdog ではない。新フロー追加時は『この系統は何層あるか・各層の故障相関は』を必ず問う**。GAS への第9トリガー (runner 起動) 追加も今後の選択肢。
- **#68 (2026-06-13) — gz 移行 (#64) の取りこぼし2件: race_day_runner が iter ごとに DB.gz を push せず結果が終日反映されない / auto_pedigree が cron で走るだけで commit/push が完全に欠落 → 血統 60%+ 欠落の真因**: 6/13 土曜、ユーザー指摘で「結果が消えた」発覚。runner は 10:31 起動 → iter=21 (13:50) まで毎10分回っていたが、ループ内 commit が `git add docs/data/` だけで `keiba.db.gz` を含まず、origin の DB は朝の予測時点で凍結 → export_predictions が同じ古い DB を読み続け JSON の `has_results=False` が終日永続。#64 で gz 化した際、最終 step だけ raw→gz に置換してループ内を忘れていた。さらに **全 workflow を audit** したところ `auto_pedigree.yml` は **cache 保存のみで commit/push step が一切なく**、毎日走っていた血統補完が cache evict のたび水の泡 → CLAUDE.md に何度も書かれた「血統 60%+ 欠落」恒常問題の真因が判明。**対策**: (1) race_day_runner のループ内 commit に `db_pack + git add keiba.db.gz` を追加、binary 衝突は `-X theirs` (= 自分の最新 DB) で解決、(2) auto_pedigree に `permissions: contents:write` と commit/push step を新設、(3) manual_predict は debug 用と意図コメントで明示、(4) 全 12 workflow を機械チェックして他に取りこぼしがないことを確認。**教訓: 「対策の対策」が要る — gz 化のような横断的リファクタは grep だけでなく『書き込み/コミット/プッシュ』の三点で各 workflow を機能ごとに照合すること。`actions/cache/save` だけで完結している workflow は origin に届かないので、そもそも cache を「永続データの一次置き場」にしてはいけない**。
- **#69 (2026-06-15) — 宝塚記念で (1) 予想◎が「ぐりぐり1人気→2着」を大舞台で再現 + 記事と予測の完全乖離、(2) 15:05 発走35分前に予想スレッド再投稿事故**: 6/14宝塚記念(重馬場)。結果は1着メイショウタバル(2人気3.9)/2着クロワデュノール(1人気2.5)/3着ダノンデサイル(3人気7.0)。**反省2点**。(A) **予測ロジックの構造欠陥**: post_predict の◎は単勝2.5倍の1人気クロワデュノール→2着。前日(6/13)の「S信頼度3戦すべて1人気で1着ゼロ・全部2-3着」と同一パターンを最大の舞台で反復。さらに致命的なのは、**自前の記事(前後編)が「重馬場ならメイショウタバルが雨シナリオの主役・クロワは道悪経験ゼロ」と正しく分析し、実際に重馬場でタバルが勝った**のに、**予測の印にはタバルが一切付いていなかった**(◎クロワ ○ダノン ▲ミュージアム △レガレイラ ×タガノ 注ビザンチン)。= 記事(オッズ非依存の能力/コース分析)と post_predict(市場人気追随)が完全に分断され、勝ち馬を無印で公開した。**教訓: 印は「人気」でなく「能力モデル+コース適性+馬場」から引くべき。記事ロジックと予測ロジックを同じ根拠に統合せよ。1人気を◎にして2-3着は『当たった』ではなく『市場のコピーで妙味ゼロ』**。(B) **変な投稿 (時間帯異常)**: 15:05 JST(宝塚発走35分前)に予想5連スレッドが再投稿された。真因は #45 で post_predict の「11時以降全スキップ」ハードガードを撤廃し「発走済みレースを個別除外」に変えたこと → 未発走の午後レース(宝塚15:40)は除外対象外で午後でも投稿された。atomic lock も #68 の gz-push 遅延で 15:05 の run に見えず素通り。**対策: cmd_predict 冒頭に「当日12:00 JST 超過なら lock 非依存で即 return」のハード上限ガードを復活** (SKIP_TIME_GUARD=1 で override)。odds_flash は既存ガード(9-11時)で15:01を正しくスキップ済みだった。**教訓: ガードを「粗いから」と緩める時は上限を必ず残す。post_predict は朝のコンテンツ投下専用で、午後発火は常に異常**。





- **#36 (2026-05-30) — 予測フラット解消 (WIN_SHARPEN) + confidence を ◎勝率ベースに再設計**: ユーザー指摘「予想勝率低い・ばらつきなさすぎ・改悪」「京都9R 本命48%なのに B はおかしい」。2 つの根因。**(1) 予測フラット**: `ml/model.py` の pred_win_norm が rank_score の素 softmax で、rank_score のばらつき (std 0.2-0.3) が小さく 16頭で本命9.5%・9頭で20% と均等化。`WIN_SHARPEN=3.0` で logit を3倍 (温度1/3) → 16頭本命18%・9頭48% と本命明確化。**(2) confidence 不整合**: v4 (#21) は composite の 75% が投資妙味 (trio_ev/odds_pot/umaren_ev) で、◎48%でもオッズ1.7倍 (妙味なし) → B。投資でなくエンタメ用途では「AI がどれだけ本命を絞れたか = ◎勝率」が confidence にふさわしい。`confidence.evaluate` を ◎勝率ベース (S≥45/A≥30/B≥20/C≥13/D<13) に変更、妙味は reason に「(参考)」併記。結果: 京都9R ◎48%→S、ダービー◎16%→D (18頭混戦を正直に反映)。confidence (予測自信度) と should_bet (投資妙味、オッズband) が綺麗に分離。**副次バグ発見**: ローカルで複数 predict プロセスを並走させると cache が交錯し flat 値で上書きされる (本番 GitHub Actions は1プロセスなので無害だが、ローカル一括再生成は順次実行が必須)。**教訓: confidence の意味を「予測自信度」か「投資妙味」か明確に。エンタメと投資で求める指標は違う。WIN_SHARPEN は表示確率を鋭くするが ROI 計算には使わない (pred_win_norm のみ、calibrated prob_win は不変)**。 **追記 (同日 ae9f5c7): ◎勝率ベース化しても「SとAが少しだけで他がほぼD」とユーザー再指摘**。真因は `confidence.evaluate` を ◎勝率ベースにした後も、`predict.py:cmd_predict` で `volatility.compute_race_volatility` の `conf_adjust` (未勝利 -2 / 1勝×ダート・ローカル・短距離 -1〜-2) を **confidence に後段適用** していたこと。例: 未勝利 ◎35% が grade A → volatility -2 で C に降格 → 分布が C/D に偏る。volatility の ROI 降格は **投資判断 (should_bet) の話**であって予測自信度ではないので、(1) predict.py から confidence への conf_adjust 適用を撤去 (reason 表示のみ残す)、(2) 未勝利・新馬の見送りは `strategy/betting.py:should_bet_race` 冒頭の race_name 判定に移設。結果 5/30 分布 {S:2,A:4,B:3,C:14,D:1}・5/31 {A:4,B:6,C:10,D:4} で **confidence と ◎勝率グレードが mismatch:0 で完全一致**、未勝利戦は A/B 表示でも bet=False。**再教訓: 「分離した」と書いても呼び出し元 (predict.py) で後段補正が残ると未分離。#21 と同じく大規模ロジック変更は全呼び出し元を grep して後段の上書きが無いか確認すること**。
- **#37 (2026-05-30) — 投稿 cron が git 競合で全 failure + watchdog 自己矛盾**: ユーザー「投稿もうまくできてないな」。調査で今朝の投稿系 run が連続 failure と判明。根因2つ。**(1) git 競合 exit 128**: `auto_post_x.yml` の Deploy / Save post history step が `git pull --rebase --autostash || true` で予測JSON競合を握りつぶし、unmerged(U) を作業ツリーに残す → 残った U 状態で後続 commit が「Committing is not possible because you have unmerged files」で exit 128 → run 全体 failure。引き金は **confidence 修正 (#36) で predictions_*.json を手動ローカル push したのと、Actions の export が同一ファイルを二重書き込み**したこと。平常日 (手動 push 無し) は競合せず成功していたため潜在化していた。**対策**: 両 step を `git pull --rebase -X theirs`(リモート優先で自動解決し U を残さない)+ 失敗時 `git rebase --abort`(残骸除去)に統一。**(2) watchdog 自己矛盾**: `posting_watchdog.yml` の health check step が `set -e` 下で `check_posting_health.py`(未投稿ありで exit 1)を握らず実行 → step が死に `missing_count` output 未設定 → 後続「未投稿 slot を自動 trigger」step が暗黙の success() 判定でスキップ → **未投稿を検知した瞬間に自動復旧が止まる**自己矛盾。**対策**: 表示用呼び出しを `|| true` で握り、後続 trigger を必ず起動。**重要**: 投稿 (Post to X) step 自体は成功しており投稿機能は正常。今日 (土) 投稿ゼロは「土曜朝 cron は予測のみ・実投稿は 10:15 post_predict から」設計通りで時間的に正常だった (誤解しやすい)。**実地検証**: 修正後 `refresh_dashboard` モード (投稿なしで Deploy/Save の git フローのみ通る) を手動 trigger → post ジョブ ✓ 完了 (修正前は同ジョブ exit 128)。**教訓: 同一ファイルを手動 push と Actions が二重書き込みする運用では `--autostash` は競合を握りつぶして U を残し、`|| true` が逆に致命傷を隠す。`-X theirs` で確定解決すべき。SLO watchdog 自身が「監視対象の失敗モード (=投稿を止める)」に陥る設計 (検知行為で自滅) を避ける。投稿が無い=故障とは限らず、まず slot 設計 (予測のみ slot か投稿 slot か) を確認する**。
- **#38 (2026-05-30) — post_predict が sqlite3.Row.get() で全 X 投稿停止 (リグレッション)**: ユーザー「今日の投稿されてないよね？」。#37 の git 競合を直した後も 10:15 post_predict run が failure。ログで `AttributeError: 'sqlite3.Row' object has no attribute 'get'` (post_x.py `cmd_predict` L593) と判明 = **投稿本体のクラッシュ** (#37 の git 競合は周辺の履歴保存で別問題)。`all_races` は DB由来 (sqlite3.Row) と GitHub Pages JSON由来 (dict) が混在しうるが、候補ソートの `r.get('should_bet')` は dict 専用で sqlite3.Row では落ちる。**混入は同日朝の e3bfbc2** (「post_predict が信頼度A取りこぼし」修正で should_bet 優先ソートを追加した際に `.get()` を使用)。前日まで正常 = リグレッション。**対策**: `all_races` 取得直後に `[dict(r) for r in all_races]` で型統一し、以降 .get()/[] を一貫使用可に。`SKIP_PREDICT_GUARD=1 ... --dry-run` で投稿テキスト正常生成を確認 (京都11R/東京11R/京都9R 信頼度S/東京10R)。**教訓: sqlite3.Row と dict を同じ変数に混在させない。DB fetch 直後に dict 化する規約。`.get()` を書くなら Row が流れ込む経路 (DB直 fetch) を必ず dict 化。型/アクセス方法を変える修正は全経路 (DB/JSON 両方) で動作確認する。リグレッションは「前日まで動いていた」ため検知が遅れる → 投稿系の変更は dry-run を commit 前の必須ゲートにすべき**。
- **#39 (2026-05-30) — 結果投稿が11Rのみで予想を網羅しない構造的不整合**: ユーザー「結果投稿内容が網羅されてない」。Explore 監査で判明: `cmd_results` (結果投稿) は `_build_results_from_json` (L758) / `_build_results_from_db` (L816) 両方で `race_number == 11` を硬コードし**11Rのみ報告**。一方 `cmd_predict` (予想投稿) は11R + 注目S/A(最大3件・最大13R) を投稿。**予想した注目レースの大半が結果報告から完全に漏れていた**。さらに結果ツイートのレース番号が `"11R"` 文字列ハードコードで他レースを出しても誤表示。**根本対策**: 予想と結果で同一の `_select_target_races(all_races)` 共通関数 (11R + S/A、should_bet優先で最大3件) を新設し、`cmd_predict` / `_build_results_from_json` / `_build_results_from_db` の3箇所すべてがこれを使用 = **予想と結果が必ず同じレース集合を扱うことを構造的に保証**。レース番号も実 `race_number` に。dry-run検証: 5/24 結果が 3R→6R に倍増、番号正しく表示、予想5/30は5Rで従来通り。**教訓: 「予想で出したもの=結果で報告するもの」のような対になる処理は、選定ロジックを1つの共通関数に集約して構造的に一致を保証する。2箇所に別々に書くと必ず乖離する (#33 の戻り値型不一致と同じ構造的教訓)**。
- **#41 (2026-06-07) — 投稿スロット排他制御 (atomic lock) を DB に実装、二重投稿を物理的に防ぐ**: ユーザー激怒「4時間前にまた予想を投稿してました。ほんといい加減にしてください。根本的にシステムの予想が反映されない。投稿がちゃんとしよう通りにされない」「cornの問題なら他に方法考えろ」。今日 6/7 (土) の X 投稿 GitHub Actions run を gh CLI で時系列調査すると、`post_predict` が **11:02 (手動) と 17:03 (watchdog 経由) の 2 回**走り、本来 10:15 専用の予想配信が午後にも投稿された。原因: (1) 既存の `post_history.json` がテキストハッシュで重複検知してたが、文面が変わると別ハッシュで擦り抜け、(2) watchdog の cron success 判定が異なる event/title だと「未投稿」と誤判定して fallback 再 trigger、(3) cron/手動/watchdog の 3 系統が独立に走り「この slot は今日まだ投稿されてないか」を atomic に決める仕組みがなかった。**Phase 1 抜本対策**: SQLite に `posted_slots(post_date TEXT, slot_name TEXT, posted_at, run_id, workflow_event, success, UNIQUE(post_date,slot_name))` テーブルを新設し、UNIQUE 制約を「物理的な二重投稿防止」の核に。`database.acquire_post_slot()` が INSERT 試行 → 成功なら lock 取得 → 失敗 (sqlite3.IntegrityError) なら **そのまま skip**。`post_x.py` の 7 cmd_* (post_predict/results/weekday/hit_flash/odds_flash/morning/evening) 冒頭に `_acquire_or_exit('<slot>')` を挿入。`auto_post_x.yml` の Deploy step で **全 post 系 type について keiba.db を git push** するように case 文を拡大 (旧版は post_predict のみ) — これにより次の cron/watchdog/手動 trigger は最新の lock 状態を見て skip 判定できる。`_POST_LOCK_DELEGATED=1` 環境変数で `cmd_hit_flash → cmd_results` の内部委譲時の重複 lock 取得を抑止。`SKIP_POST_LOCK=1` で緊急復旧用 bypass。再投稿が必要な場合は `DELETE FROM posted_slots WHERE post_date=date('now','+9 hours') AND slot_name='<slot>'` で手動解除。**Phase 2 (同日続けて、deep audit 由来)**: (a) `race_day_runner.yml` の trigger_post() に posted_slots 事前 check 追加 — `/tmp/race_day_post_<DATE>.flags` が run-scoped で消えるため、遅延 cron 発火時に flags が空で再 dispatch していた (今回 17:03 二重投稿の主犯)。posted_slots は run 間で共有される atomic lock なので事前 check で skip。(b) `posting_watchdog.yml` の自動 trigger に posted_slots 事前 check 追加 — watchdog の missing 判定が `display_title='cron'` で hit 失敗して誤検知 → fallback dispatch していた。`database.is_slot_posted()` で物理 lock 状態を確認、locked なら skip。(c) `seal_predictions_for_date()` / `is_prediction_sealed()` を quality-aware に変更 — 朝 07:00 で flat predict が seal されると --force でも修正不能になり、6/7 24R 全 D が長時間固定された。`_is_degenerate_predictions()` で max(pred_win_pct) < 12% を degenerate と判定し、seal せず再 predict 可能を保つ escape hatch。(d) `preflight_check.py` に `check_ml_output_sanity()` 追加 — 50%+ レースで max ◎勝率<12% なら critical / 30%+ なら warning、auto-fix で predict --force を発動。「24R 全 D」を seal 前に検知して自動再 predict を強制。**残課題 (Phase 3 = Task #38/#41/#42/#43)**: `scraper.py:scrape_shutuba` が weight 列を parse しない設計 → #25 馬体重変化補正は全レースで dead code (6/7 全 357 頭 weight=0 の原因)。scrape pipeline 全体の silent failure audit (R1-R10 の silent skip、refresh_entries の馬番ズレ、deploy push race)。cron 代替 trigger 経路の選定 (cron-job.org/GAS/Cloudflare Workers)。sanity monitoring の常時化。**教訓: 「気をつける」「重複防止 hash で投稿減らす」では再発する。投稿の冪等性は DB レベルの UNIQUE 制約で物理保証すべき。文面 hash の重複検知は文面が変わると擦り抜けるので invariant にならない。同じ slot を意味する key (date+slot_name) を不変式の中心に置く。/tmp の run-scoped state は run 間共有の atomic lock の代替にならない。seal の lockout は escape hatch (quality check) を持たせないと、朝の bad predict が一日中固定される**。
- **#40 (2026-05-30) — Threads token 60日失効で投稿停止 + 自動更新の仕組み**: #38 調査中に発見。手動復旧で X 投稿は7件成功したが Threads は `Session has expired on 24-May-26` で全失敗 = long-lived token が5/24に失効し約1週間 Threads 未投稿だった。**Threads (Meta) の long-lived token は60日有効、`refresh_access_token` を失効前 (24h経過後〜60日以内) に呼ぶと約60日延長**できる。失効後は refresh 不可 → ブラウザ再認証 (手動) が必要。**対策**: `scripts/refresh_threads_token.py` (refresh API、標準ライブラリのみ) + `.github/workflows/refresh_threads_token.yml` (週次 cron → `gh secret set` で自動更新、失敗時 Issue 通知)。前提 (1回設定): 有効な THREADS_ACCESS_TOKEN + Secrets read/write 権限の THREADS_TOKEN_PAT (GITHUB_TOKEN では secrets を書けないため PAT 必須)。**教訓: 外部 API の long-lived token は「失効する」前提で自動更新を組む。X (tweepy/OAuth1.0a で長命) と違い Threads (Meta Graph, 60日) は明示的 refresh が要る。token 系の失敗は「片方だけ成功」で気づきにくいので投稿ログで X/Threads 両方の成否を必ず出す**。
- **#41 (2026-05-30) — 結果投稿の「除外」誤表示 + 予想レース不一致を根本解決**: ユーザー「印に除外はあり得ない」「予想と同じレース全てを結果配信して」。調査で2根因。**(1) 着順欠落で「除外」誤表示**: `cmd_results` は JSON-first だが、結果確定後の JSON 再 export が漏れており 11R 以外の `finish` が None → 印馬が全部「除外」と誤表示されていた。一方 DB の `results` テーブルには全レース全馬の着順が確実にあった。**対策**: cmd_results を DB-first 化 (着順は DB の results が source of truth、JSON はフォールバック)。**(2) 完走ガードが posted_at 依存で素通り**: `seal_predictions_for_date` は posted_at を立てるはずだが、GitHub Actions で seal した DB はローカル/git に反映されず posted_at が事実上記録されない → 完走待ちガード (posted_at IS NOT NULL のレースを待つ) が「対象0件」で素通りしていた。**対策**: ガードを posted_at 非依存にし、`_select_target_races` (予想と同じ選定) の対象レース完走状況で直接判定 → 予想した全レース確定後に一括報告を保証。dry-run (5/24): 「除外」0件、◎○▲△×注 全6印に着順、6レース (11R3+S/A3) 網羅。**教訓: データの source of truth を1つに定める (着順は results テーブルが原本、JSON は派生ビュー)。派生データは更新漏れで古くなるので、正確性が要る箇所は必ず原本(DB)を引く。状態フラグ(posted_at)依存の設計は、そのフラグが確実に立つ保証が無いと素通りする — フラグでなく実データ(完走状況)で判定する方が頑健**。残課題: export_predictions.py が結果確定後に再実行されず JSON の finish が古い (ダッシュボード結果表示に影響、結果投稿自体は DB-first で回避済み)。
- **#42 (2026-05-31) — 結果投稿変更の整合性監査 + 補助投稿の選定統一**: ユーザー「システムの影響範囲・整合性を確認して」。#39-41 で cmd_predict/cmd_results を `_select_target_races`(11R+注目S/A)に統一したのを受け Explore で全 slot を監査。**✅ 整合確認**: 予想・結果・的中速報(`cmd_hit_flash` は `cmd_results` に委譲)・データ構造(JSON/DB 両経路で race_id/confidence/should_bet 揃う)・posted_at 廃止の副作用なし(seal は database.py 内部のみ)・fact_check/文字数 OK。実データで予想=結果が 5/31 に5レース完全一致を確認。**⚠️ 既存乖離を発見**(今回の変更が壊したのでなく元からの不統一): `cmd_odds_flash` / `generate_weekly_summary` / `generate_weekend_preview` が「11R のみ」のままで予想・結果と乖離。**対処(ユーザー選択=結果系優先)**: 結果系の `generate_weekly_summary`(月曜の先週末成績)を `_select_target_races`(各日)に統一 → 予想した注目レースの成績も集計(5/30-31 で 4→10レース)。オッズ速報・週末プレビューは「メイン速報/予告」の役割なので現状維持。**残課題1**: 月曜サマリーは先週末の predictions_cache に依存するが `flush_old_cache` が当日 cron で先週末分を削除しており、月曜にはデータが無い(5/24-25 が 5/31 時点で flush 済) → flush 保持期間を延ばす必要。**残課題2**: confidence が予想後の再予測で変わると予想・結果の選定がズレる理論リスク(再予測しなければ問題なし)。**教訓: 共通関数で主要フローを統一しても、同じ選定を独自実装している補助箇所が残ると一貫性が崩れる。grep で `race_number = 11` 等を全数チェックし、役割が同じものは共通関数に寄せる**。
- **#43 (2026-05-31) — 出走馬の幽霊重複登録を全経路で根絶**: ユーザー「オッズ反映してない馬がいる」。調査で出走表に存在しない幽霊馬(オッズ0)を 5/31 の5レース7頭検出。ダービー本来18頭がDB20頭(19-20番が11/17番リアライズシリウス・ロブチェンの重複)、目黒14頭が16頭。**幽霊馬が予測の印に混入(目黒▲レヴォントゥレット=出走しない馬を推奨)し予測を歪めていた**。**根本原因**: `scrape_shutuba` が枠順確定前(月曜)に netkeiba 出馬表の行数で仮馬番 1..N を採番。確定後の正式馬番 1..M (M<N) で `INSERT OR REPLACE` しても M+1..N は新規レコード扱いで削除されず残存。`UNIQUE(race_id,horse_number)` は同一馬の異馬番重複を防げない設計欠陥。**修正**: 出走馬保存の全3経路 (`scraper.save_race_to_db` / `predict.py:cmd_predict` / `post_x.py` 金曜投稿) に「最新スクレイプの馬番に無い結果未確定(finish_position=0)レコードを削除して完全同期」を追加 = 確定済着順は保護しつつ枠順前の仮馬番を一掃。既存幽霊7頭は netkeiba オッズAPI(実出走馬)基準で除去 → 5/31 再予測で全馬実オッズ反映・幽霊印解消(目黒▲が実馬キングスコールに)。**教訓: スクレイプ由来データは「追加」でなく「最新で完全置換」を基本にする。UNIQUE 制約は単一カラム値の重複しか防がない — エンティティ(馬)の同一性は別キー(horse_id)で、「最新スナップショットに無いものは消す」同期削除で担保する。netkeiba 出馬表の行数は枠順確定前に余分な行を含む(仮番採番の罠)**。残課題: 過去レースにも同種幽霊が残存しうる(別タスクで一括検査)。preflight_check に「APIの実頭数 vs DB頭数」照合を追加すべき。
- **#44 (2026-05-31) — 馬番と馬名のズレ (枠順確定前の仮馬番が残存)**: ユーザー「馬番と名前が合ってない」。23/24レースで馬番が netkeiba 確定馬番とズレ(294件)。原因: `scrape_shutuba` は枠順確定前(月曜)に出馬表の馬番欄が空のため五十音順で仮馬番を採番。枠順抽選(金11時)後に確定馬番へ再 scrape されず仮番のまま予測・投稿されていた(ダービーのみ偶然一致)。**修正**: `refresh_entries.py` 新規(全レースを scrape_shutuba で再取得し save_race_to_db で確定馬番に同期)。`auto_post_x.yml` の predict/odds_flash 予測前に組込み。5/31 を確定馬番で再生成(DB内部 馬名不一致0・オッズ欠落0)。**重大な反省点2つ**: (1) #43 の幽霊除去時に馬番と馬名の整合性を確認しなかった → ユーザー指摘「修正する時ちゃんと仕様と合ってるか整合性取れてるか毎回確認してよ」。(2) keiba.db 修正中に `git pull --rebase -X ours` で Actions の仮馬番版に上書きされ修正消失 → `-X theirs`(ローカル優先)で再 push。**教訓: スクレイプ由来データは確定タイミング(枠順確定後)で必ず再取得する。データ修正後は必ず一次ソース(netkeiba)と照合してから「完了」とする — 修正の度に整合性チェックを習慣化。keiba.db のような Actions も書く binary の競合は -X theirs (ローカル優先) で解決し、DB書込中の安易な pull を避ける(#11 再確認)**。
- **#45 (2026-05-31) — 過ぎたレースを投稿しない + post_predict 未発火**: ユーザー「投稿されない」「過ぎたレースは投稿しないでよ」。2問題。**(1) post_predict が走らない**: 土日10:15 の cron はあるが GitHub Actions cron 遅延(#22)で未発火 → 手動 trigger で投稿。**(2) 発走済みレースを投稿していた**: post_predict の時間ガードが「当日11時以降は全スキップ」と粗く、11時前は午前の発走済みレースも投稿、11時以降は未発走の午後レースも投稿不能だった。**修正**: 時刻ベース全体スキップ(11時以降skip)を撤廃し、`_select_target_races` の後で `start_time < 現在時刻` のレースを個別除外。「常に未発走レースのみ投稿」に。本番(10:54)で午前2レース除外・ダービー+白百合Sのみ投稿を確認(X 4ツイート成功)。**教訓: 時間ガードは「全体 on/off」でなく「対象を絞る」方が正確。投稿対象の鮮度(発走前か)はレース単位で判定する。cron 遅延に備え post_predict の watchdog 補完も要検討**。Threads は token 期限切れのまま(#40 の再発行待ち)。
- **#46 (2026-05-31) — 結果投稿が「予想投稿していないレース」まで報告 (重大ルール違反)**: ユーザー強い指摘「投稿したレースしか結果は投稿しないと言ったよね。予想投稿していないレースまで投稿されてる」。原因: 結果投稿 (_build_results_from_db/json + 完走ガード) が `_select_target_races` で「11R+S/A」を**毎回再計算**していた。発走済みレース除外(#45)で予想を2レース(ダービー+白百合S)に絞っても、結果投稿は再計算で全 11R+S/A を対象にし、予想していない東京1R/2R(発走済み)の結果まで報告していた。**本質**: 「予想したレース」を**再計算で推定**していたため、時刻依存の発走済み除外を反映できずズレた。**修正**: (1) post_predict が投稿成功時に実際に投稿した target_races の `posted_at` を記録、(2) 結果投稿3箇所 (_build_results_from_db / _build_results_from_json / 完走ガード) を `posted_at IS NOT NULL` のレースのみ対象に変更。「実際に投稿したレースの記録」を唯一の正とする。検証: 結果対象が京都11R+東京11Rの2レースのみに。**教訓: 「予想したレース=結果報告するレース」のような対の処理は、選定ロジックを2度実行(再計算)してはいけない。1度目(予想投稿)で実際に投稿したものを記録(posted_at)し、2度目(結果)はその記録だけを読む。再計算は時刻・状態に依存してズレる。#39 で共通関数化したが「再計算」である限り発走済み除外(#45)でズレた → 状態を持つ処理は記録ベースが正解**。
- **#47 (2026-06-01) — 「投稿されない」の最終真因は X API write の Cloudflare bot判定**: ユーザー「投稿されない」が連日続き、多層的な原因を順に除去。(1) fact_check がデータ系投稿(コース分析の「複勝率75%(15/20)」等)を「具体的な馬名/馬番が含まれない=中身なし」で誤ブロック → キーワードのホワイトリストに加え統計データの実体(率%2個 or 率+サンプル数(N/M) or 着度数[N-N-N-N] or ランキング)で「中身あり」判定。(2) 手動 trigger の skip_guard が SKIP_PREDICT_GUARD のみに渡り cmd_morning の SKIP_TIME_GUARD に届かず時間ガードで skip → workflow で SKIP_TIME_GUARD も渡す。(3) **最終真因**: `create_tweet` (write/POST) が 403 "Just a moment"(Cloudflare JSチャレンジHTML)でブロック。read(GET)は通るのに write が落ちる = tweepy デフォルトの User-Agent(python-requests/...)が X の Cloudflare に bot 判定されていた。**`load_x_client` で requests session(client.session / _v1_api.session)に ブラウザ風 User-Agent(Chrome)を設定 → Cloudflare 通過 → 投稿成功**(morning 3ツイート ID付き確認)。**教訓: 403 のレスポンスボディを必ず見る — JSON{errors}なら API 認証/権限、HTML"Just a moment"なら Cloudflare bot対策で User-Agent 偽装で回避できる。read成功/write失敗の非対称は GET/POST への Cloudflare 判定差。「投稿されない」は単一原因と決めつけず層ごとに潰す(#37 git競合 → #38 sqlite3.Row → #46 posted_at → #45 発走済み → #47 fact_check/時間ガード/Cloudflare と多層だった)**。Threads は token 期限切れ(#40)のまま要再発行 → **2026-06-04 復旧完了**: Meta Developer Portal でユーザーが long-lived token (60日) を再発行、claude-in-chrome での画面操作支援を経て GitHub Secrets `THREADS_ACCESS_TOKEN` に設定、テスト投稿成功 (post_id 17862670266633219)。週次 refresh_threads_token workflow が60日ごとに自動延長するので以降は失効しない。
- **#48 (2026-06-02) — morning投稿のX重複拒否(duplicate content)**: #47 で Cloudflare を解決した翌日、6/2 の morning が 403 "You are not allowed to create a Tweet with duplicate content"。原因: morning(`build_morning_post`)は毎日「今週末メイン」の同じレース・同じ3セクション(歴代勝ち馬/1人気信頼度/種牡馬)を生成し、header の day もレース開催日(固定)なので、月曜と火曜で完全に同一テキスト → X が重複拒否(同一テキストは24h以内に拒否される)。**対症**: header に投稿日「N/N(曜)時点のデータ」を追加してテキストをユニーク化(オッズ・予測は日々更新されるので情報としても妥当)。6/2 morning 3ツイート投稿成功。**残課題(根本)**: morning に曜日別バリエーションが無い(曜日別 builder は evening系のみ)。post_sections には18個の sec_*(prev_race_pattern/outlier_year/pace_decisive/jockey_recent_form/post_position_bias/dangerous_favorites 等)があるので、曜日でセクションをローテーションして毎日違う切り口にすべき(別タスク)。**教訓: 同じ対象を毎日投稿する slot は「日替わりの差分」を必ず持たせる。連日の「投稿されない」は #37→#38→#45→#46→#47→#48 と多層で、一つ直すと次が出る性質だった — X投稿パイプラインは(cron→時間ガード→fact_check→認証/Cloudflare→重複)の各層を独立に検証する**。
- **#49 (2026-06-02) — morning投稿の曜日別ローテで重複を根本解決**: #48 の残課題(根本)を実装。`slot_composer.build_morning_post` を曜日(weekday 0=月〜6=日)でセクション集合を切り替える方式に再設計。`_MORNING_ROTATION` dict に曜日→{emoji/theme/sections/cta} を declarative に定義し、各 `_morning_sec_*` ヘルパー(historical/pop_trust/sires_compact/sires_detailed/pattern/age/jockey/pace/post_pos/danger/outlier)が `(section_str|None, n)` を返す。月=週末ラインナップ(歴代×1人気×種牡馬)/火=血統(パターン×種牡馬×馬齢)/土=本番直前(危険な1人気×枠順)/日=波乱とコース傾向。**重要な設計上の罠と対策**: (1) **thread 分割で各セクションは単独 tweet になり得る**ので、どのセクションも単独で fact_check(post_x.has_data_substance)を通る形=率%×2 か 率+サンプル数(N/M) か 馬名(年+カナ)で返すこと。`sec_rotation_pattern`/`sec_prev_race_pattern` は %もカナ馬名も無い行(「🏆中4週: 2勝/3」)を出すので単独 tweet で fact_check 落ち→**採用見送り**。(2) **`該当馬なし`/`該当する` は fact_check の empty_pattern に当たり 🚫 ブロック**するので、sire 行は `_clean_sire_match` で「該当馬なし」除去・「該当: X」圧縮、dangerous は「→ 該当する〜」行を出さず flop率+🚨事例のみ採用。(3) **隣接 active 日(月↔火, 土↔日)はセクション集合を重複させない**=同一テキスト tweet を作らない。先頭 section は必ず日替わり header(emoji+theme+投稿日)と同じ tweet に入るので、フォールバック時に隣接日と同 section でも tweet 全文は重複しない。**検証**: 安田記念で全7曜日 dry-run → 16 tweets 全てユニーク・全て fact_check 2層(db_fact_check+fact_check_tweet)通過・曜日別に切り口が異なることを確認。さらに高松宮/フェブラリー/天皇賞春(3200m薄データ)/障害戦/OP芝で graceful degradation(薄い日は fallback:historical で空投稿回避)を確認。実運用では水木金朝は専用 builder(build_wed/thu/fri_morning_post)へルーティングされるため build_morning_post が実際に使うのは月火土日。`build_morning_post(race, conn, today=...)` に `today` 注入を追加(曜日別テストのため)。**教訓: #48 の「日替わりの差分を持たせる」を header の暫定回避から構造化。thread 化された投稿は『各 tweet が単独で fact_check を通るか』を全 tweet で検証する(セクション単位でなく分割後の tweet 単位)。「該当馬なし」系の内部メッセージは fact_check ブロック語彙なので投稿テキストに混ぜない**。

- **#50 (2026-06-02) — ヘッダーと中身の不一致(タイトル詐欺) + 中身ゼロ投稿**: ユーザー「追い切り評価と書きながら何も評価してない、意味わからん」。`build_wed_weekday_post`(水昼) が `header="🏋️追い切り評価"` 固定なのに、未来レースは追い切りが直前まで非公開で `sec_workout_focus` が空 → `sec_handpicked_top`(コース適性) をフォールバック表示するため、タイトルと中身が食い違っていた。**修正**: ヘッダーを「実際に出るセクション」に合わせて動的化 (追い切りデータ有→「追い切り評価」/ 無→「コース適性 注目馬TOP」)。**全 builder 監査で判明した追加問題**: (a) `build_thu_morning_post`(木朝「出走馬×コース適性」) は出走馬未確定時に entry_pedigree/sires が両方空 → ヘッダー+CTA だけの**中身ゼロ投稿**(#1 の最悪パターン)、(b) `build_thu/fri_weekday_post`「注目馬TOP3」がコース適性 TOP を出していてテーマと微妙にずれ。**教訓: ヘッダー(タイトル)はハードコードせず、実際に採用されたセクションから決める。フォールバックでセクションが変わるならヘッダーも変える。セクション全滅時はヘッダー+CTA だけの空投稿を出さず、ほぼ常に取れる historical/コース適性 で埋めるか投稿自体をスキップする。「タイトルと中身の一致」は fact_check と同格の投稿前チェックにすべき**。全 builder への横展開は別タスクで一括対応 → **#51 で完了**。
- **#51 (2026-06-02) — 全 builder のタイトル詐欺と中身ゼロ投稿を一掃 (#50 横展開完了)**: #50 の方式 (動的ヘッダー + フォールバック + return None) を全 morning/weekday/evening builder に展開。修正対象: `build_thu_morning_post`(出走馬未確定で中身ゼロ→歴代→コース適性) / `build_thu_weekday_post`+`build_fri_weekday_post`(「注目馬TOP3」固定→AI予測有/handpicked で動的) / `build_wed_morning_post`(騎手薄コースで全滅→歴代→種牡馬) / `build_fri_morning_post`(パターン/種牡馬両空対応+テーマ動的化) / `build_morning_post`(月火土日 安全網後も空なら skip) / `build_wed_weekday_post`(#50 残ギャップ) / `build_thu_evening_post`(同型欠陥)。`post_x.py:_build_weekday_post` も `tweets=None`(意図的スキップ)を旧 weekday_engine にフォールバックさせない `(None, race_id)` 返却に。**検証**: 7レース(G1安田記念pc=0/OP水無月S/天皇賞春3200m薄/1勝特別/3勝ダート/未勝利pc有/OP白百合Spc有)× 13 builder = **91パターン全通過**(修正前4失敗→修正後0失敗)。(1)ヘッダー2行目テーマ↔本文【】見出し整合 91/91、(2)中身ゼロでない 91/91、(3)fact_check 2層 全 tweet 個別通過 91/91。**教訓: 「タイトルと中身の一致」+「中身ゼロ投稿を出さない」は fact_check と同格の構造的保証。フォールバックでセクションが変わるならヘッダーも変える、ヘッダー+CTA だけになるなら投稿自体を skip する原則を全 builder で統一**。
- **#52 (2026-06-08) — 「朝に予想が反映されない」+「全レースD化」の2大根本原因を確定し4点修正**: ユーザー「システムがわも検査して。毎回データが朝に反映してないよね。予想ロジックもおかしくなってたし」。2エージェントで deep audit + 自分でコード行・実証検証。**根本原因①「毎回朝に反映されない」= push 消失**: `auto_post_x.yml` の Deploy/Save step (L542/L564) だけが `git pull --rebase -X theirs` で **`--autostash` が無い**。commit 後も 100MB の keiba.db が WAL flush で unstaged に戻り、autostash 無しの rebase は「you have unstaged changes」で即死 → 6/6 は predict 24/24 成功・export 成功・commit `a245077` 成功なのに push が3回とも死に、予測JSONが origin に届かず**朝404が継続**。他の全 workflow は `--autostash` 付きで生存していた = auto_post_x だけの欠陥。**根本原因②「全レースD化」= 予測時に odds=0**: `model_rank.pkl` は **gain の 92% が odds_log** (オッズ依存)。実証: 安田記念で odds正常時 rank_score std=0.273・本命29.2% → **odds=0 にすると std=0.022 (13分の1)・本命12.5%** = フラット化し `_is_degenerate` (<12%) に全頭該当 → 全レース confidence=D。odds=0 になる経路: `scrape_shutuba` (出馬表) は odds を持たないのに、`save_race_to_db` の `INSERT OR REPLACE` が既存 odds を 0 で上書き。morning step は `refresh_entries` (odds→0) → **refresh_odds を挟まず** → `predict` の順で、この瞬間に odds=0 を読んで全 D 化していた。「2回目の predict で正常化」した非決定性の正体は、その間に refresh_odds か API がオッズを復元したから = モデルは決定論的 (3回連続 std一致確認済) で、唯一の変数は予測時点の odds の有無だった。**4点修正 (A/B/C/D 全実装・テスト済)**: (A) `auto_post_x.yml` の push 2箇所に `--autostash` 追加 → 朝の push 消失を解消。(B) morning step で `refresh_entries` の直後・`predict` の前に `refresh_odds` を必ず実行 → 予測前に odds 復元。(C) `scraper.py:save_race_to_db` を `INSERT OR REPLACE` から `ON CONFLICT(race_id,horse_number) DO UPDATE` の**保全 UPSERT** に変更 — 各列「新値が非ゼロ/非空ならそれ、ゼロ/空なら既存値を保持」 → 出馬表 save が既存 odds/popularity/weight/着順 を二度と 0 で壊さない (#38 の馬体重 dead code も同時解決)。テスト: 出馬表save (odds=0) で既存 odds 7.9/392.1/35.7 保全、結果save (実値) で odds=3.5・weight=480・finish=1 に正しく更新。(D) `predict.py:cmd_predict` の `predict_race` 直前に **odds 充足ゲート** — 出走馬の odds が全頭0なら予測前に API 取得を試み、なお0なら degenerate を書かず skip して既存 cache 保持 (`--allow-zero-odds` で無効化可、履歴backtest用)。テスト: odds=0 + API不能 → 「degenerate予測を避けて skip」、odds正常 → ◎22.3% B で正常予測。**教訓: (1) 巨大binary (keiba.db) を JSON と同一 commit で push する設計では `--autostash` 必須、WAL で必ず unstaged に戻る。(2) モデルの特徴量依存度 (gain) を把握する — odds_log 92% 依存なら odds 欠損は即フラット化。「非決定的に見えるバグ」の多くは隠れた状態 (この場合 DB の odds) の有無であり、モデル自体は決定論的。(3) データ破壊は INSERT OR REPLACE が容疑者。出馬表(部分データ)と結果(全データ)が同じ行を書く設計では、部分データが全データを 0 で上書きしないよう「非ゼロ優先」の UPSERT にする。(4) 92% odds 依存は別の問題も示唆 = モデルが市場オッズをなぞってるだけかもしれない → 将来 odds 非依存の予測力を上げる課題 (Phase 4)**。
- **#53 (2026-06-08) — 平日投稿を「今週の出走馬から注目馬を実名ピックアップ」に再設計 (中身ゼロ撲滅 + フォロワー獲得)**: ユーザー「投稿の内容がやはり中身がない。必ずデータを元に今週の出走馬から注目馬としてピックアップする形にして。フォロワーが全然増えてない」。dry-run で全曜日の朝投稿を確認 → **月=歴代勝ち馬(過去の馬)、火=種牡馬見出しのみ空、水=騎手統計(どの馬か不明)、木=枠順統計、金=種牡馬パターン** と、ほぼ全部が「レースについて語る」抽象統計で**今週の出走馬を実名で挙げていなかった**。一方 `sec_handpicked_top` (各出走馬を父コース複勝率+騎手コース複勝率でスコア化し実名TOP3) は美しい実名ピックを出すのに**朝のローテに一切使われていなかった**のが構造的原因。**対策**: (1) `post_sections.py` に `sec_notable_horses(mode=combined/blood/jockey)` を新設 — 今週の出走馬を mode 別 (総合/血統/鞍上) に実名+数値根拠でTOP3抽出。AI予測不要なので出走馬確定後いつでも出せる。枠順抽選(金11時)前は `show_number=False` で馬番を伏せ馬名のみ (JRA ルール遵守)。(2) `slot_composer.py` の朝ローテ全曜日 + 専用ビルダー(水朝/金朝) + 全夜ビルダー(月火水夜) + 月夜default の先頭に注目馬セクションを配置。曜日で mode を変えて重複回避 (月combined/火blood/水jockey/木combined/金blood)。(3) 注目馬がリードの日は冗長な lede (種牡馬統計フック) を省く — lede が文字数を食い `_split_to_thread` が末尾から馬名行を削り見出しだけ残す事故 (火曜の空セクション) を解消。(4) `_split_to_thread` に「trim で見出しだけになったセクションは丸ごと落とす」安全網。(5) `fact_check_tweet(require_horse=)` 追加 + `fact_check_tweet_or_thread` を**スレッド全体で馬名有無を評価**するよう変更 — スレッドは X 上で一体表示されるので、tweet1 に注目馬があればローテ等の統計補足 tweet が単独で「馬名なし」ブロックされるのを防ぐ (夜の3 tweet 全通過)。(6) fact_check の馬名検出 regex に `[ァ-ヴー]{3,}:` (注目馬「馬名: 根拠」形式、馬番なし) を追加 — 枠確定前の注目馬を馬名と認識。**検証**: 6/14 宝塚記念 G1 で月〜日の朝(rotation+専用)+月火水木金夜+水木金昼 = 全 slot で実名注目馬が出て fact_check 通過 (修正前は火朝・水朝・金朝・火夜が空 or ブロック)。例: 月朝「1️⃣マイネルエンペラー: 父ゴールドシップ38%/川田50% 2️⃣メイショウタバル 3️⃣ミステリーウェイ」、水朝(鞍上)「マイネルエンペラー: 川田50%(10走)」。**教訓: 「中身がない」の正体は "レースについて語る統計" と "出走馬を実名で挙げる" の混同。フォロワーが欲しいのは「今週どの馬を買えばいいか」= 実名+根拠。既に良いビルダー(handpicked)があっても主要動線に組み込まれていなければ意味がない。新セクションは fact_check の馬名検出パターン・文字数予算・スレッド分割の3つと必ず整合させる (1つでも漏れると空投稿 or ブロック)**。残: 朝投稿の定刻反映 (cron遅延、#52 Phase3)、モデルの odds 非依存予測力 (#52 Phase4)。

- **#54 (2026-06-09) — 「N勝/M」分数表記を機械ブロック化 (再発を物理的に不可能に)**: ユーザー激怒「今日の投稿で1勝/5とかあるけど、これ絶対やめてって言ったよね。なんでまた同じことするの」。#53 で夜ビルダー(tue_evening 等)に注目馬リードを足した際、CLAUDE.md #49 で「採用見送り」と決めたはずの `sec_prev_race_pattern` / `sec_rotation_pattern`(「🏆春雷S: 2勝/5」のような勝数分数を出す)を一緒に復活させてしまった。**根本対策 (#24 の教訓「気をつけるでなくコードで違反を不可能に」を徹底)**: (1) `post_x.py:fact_check_tweet` に **「N勝/M」「N/M勝」を検出したら critical ブロック**する機械チェックを追加(`\d+勝/\d+` と `\d+/\d+勝`)。これで生成箇所を直し忘れても投稿前に必ず止まる。「複勝率75%(15/20)」のような %+サンプル数の括弧表記は対象外(勝敗を直接分数化したものだけ禁止)。(2) 生成箇所を全て自然表記に修正: `sec_prev_race_pattern`(2箇所)・`sec_rotation_pattern`(2箇所)・`sec_pop_trust_trend`・`build_evening_post`(危険な1人気の4箇所)を「過去N年でX勝」「N走中X勝」等に。**検証**: 全 11 builder × 2 race + 朝 rotation 7曜日 = 29 パターンで「N勝/M」ゼロ・fact_check 全通過。**教訓: ユーザーが「絶対やめて」と言った表記は fact_check で物理ブロックする(verify_article の記事チェックと同じ思想を X 投稿にも)。一度「採用見送り」にしたセクションを別の改修で復活させないため、禁止事項は『コメントで覚える』でなく『機械が拒否する』形にする。#53 の様に builder にセクションを足す時は、そのセクションが過去に何故見送られたか (CLAUDE.md 履歴) を必ず確認する**。
- **#55 (2026-06-10) — 成績表記を競馬標準の着度数 [1-2-3-着外] に全面統一**: ユーザー「書くな、競馬の書き方にして」。#54 で「N勝/M」を「過去N年でX勝」に直したが、それも独自表記でユーザーは競馬慣習の**着度数 [1着-2着-3着-着外]** を要求 (CLAUDE.md #5 で既出: 「競馬慣習は着度数」)。**対応**: `post_sections.py` に `_chakudosu(p1,p2,p3,total)` ヘルパー新設、成績を表示する全セクションのクエリに 1着/2着/3着 の個別集計 (`SUM(CASE WHEN finish_position=N...)`) を追加し着度数化。対象: `sec_pop_trust_trend`(1番人気)/`sec_jockey_recent_form`(騎手)/`sec_sire_course_cross`(種牡馬・昼の主力)/`sec_pace_decisive`(末脚)/`sec_age_pattern`(馬齢)/`sec_pattern_discovery`(好相性パターン)。例:「1番人気 [3-1-0-4] 複勝率50%」「川田 [3-1-1-5] 複勝50%」「ジャスタウェイ [0-0-2-2] 複勝50% → 該当: ミステリーウェイ」。lede(フック文)は着度数が冗長なので `(N走中M複)` を削り複勝率%のみに簡潔化。「勝ち馬の前走経由/ローテ」は勝ち馬(1着)しか数えない集計なので着度数不可 → 「N頭」表記に。**着度数 [N-N-N-N] は fact_check の `has_data_substance` が既に「中身あり」と認識**するので投稿が通る。**検証**: 全11 builder×2レース+朝7曜日=29パターンで着度数13投稿・「勝/M」0・「(N走中M複)」0・fact_checkブロック0。**教訓: 競馬コンテンツの成績は必ず着度数 [1-2-3-着外]。「N勝/M」「N走中M複」「過去N年でX勝」は全て独自表記でNG。ユーザーが業界慣習を指定したら独自に言い換えず慣習通りに。着度数は finish_position の 1/2/3 個別集計が必要 (top3 集計だけでは作れない)**。
- **#56 (2026-06-10) — 平日投稿の atomic lock が origin に届かず + 朝 cron 未発火を「冗長 cron + 直列化」で構造解決**: ユーザー「明日はちゃんと」(6/10 朝: 7:30 の morning cron が 8時過ぎても未発火。前日 6/9 昼は cron 未発火をユーザー自ら手動 dispatch で救済していた)。調査で2問題を確定。**(1) #41 の修正漏れ (片肺修正)**: Deploy step は step **内**の case 文に morning|weekday|evening を含むが、step レベルの `if` が旧リスト (predict/odds_flash 等のみ) のままで、**平日3スロットは Deploy step ごと skip = posted_slots lock が origin に push されない**。証拠: DB の posted_slots が 6/7 (週末) で停止、6/8-6/9 (平日) のロック行が無い。6/9 夜に実害寸前: watchdog dispatch が 22:24 に 3 tweets 投稿 → 22:45 の遅延 cron が再び lock 取得に成功しツイート生成まで進行、post_history の文面ハッシュ重複検出 (「0時間20分前に投稿済み」) が偶然止めただけ — #41 で「文面が変わると擦り抜ける、invariant にならない」とされた最後の砦頼みだった。**修正: Deploy step の if に morning/weekday/evening を追加** (lock が毎回 push される)。**(2) cron 未発火/遅延の構造対策**: GitHub Actions cron はスキップ・1-4.5h 遅延が常態 (#22/#36)、watchdog (*/30 cron) も実測 ~2h 間隔でしか発火しない (6/10 朝は 7:01 の次が 9時台)。lock が push される前提が整ったので、**平日3スロットに冗長 cron を追加 (朝 8:10 / 昼 13:10 / 夜 20:40 JST、dedup_cron と determine の両 case 文にマッピング追加)** — 1本目が成功済みなら2本目は lock を見て skip するだけ、1本目未発火なら2本目が投稿 = watchdog より速い自己復旧層。**併せて workflow レベル concurrency (group: auto-post-x, cancel-in-progress: false) を追加**し、多重発火が並走して「両方 lock 無し DB を checkout する」レースを直列化で排除。ローカル keiba.db の UU 残骸 (過去マージの未解決 index。working tree は HEAD と同一バイトだったので無害) も `git checkout HEAD -- keiba.db` で解消。**教訓: (1) 「case 文に追加した」は step の if が skip するなら dead code。条件は外側 (step if) → 内側 (case) の全層を確認する — #33/#41 と同型の『外側の条件』見落とし。(2) 物理 lock は「次の実行者から見える」ことまで含めて初めて invariant。push されない lock は無いのと同じ。(3) cron の信頼性は「1本 + 監視で復旧」より「冗長 cron + 冪等性 (lock)」が速くて頑健 — 冪等性が物理保証されていれば多重発火は安全側に倒れる**。

- **#57 (2026-06-10) — 仕様棚卸しで「ねじれ」2件を確定除去: hit_flash 削除 + GAS 残骸掃除**: ユーザー「すべて仕様を整理して洗い出して無駄なものは消し、必要なものは改修したい」→ 全14 workflow / 全モジュール / セーフガード15層を棚卸し → ねじれ候補を事実検証。**(1) hit_flash (土日15:30) は構造的 no-op と確定**: cmd_hit_flash は cmd_results に完全委譲 → cmd_results には完走ガード (予想投稿レース全完走まで skip) → メイン11R 発走は 15:35-15:45 = **15:30 時点で完走は物理的に不可能 → 一度も投稿せず cron/SLO/watchdog/lock を消費するだけの飾り**だった。完全削除: auto_post_x.yml (cron + dedup_cron/determine の case + fromJSON×3 + elif 分岐 + Collect results step) / posting_slo.yml slot / race_day_runner.yml trigger_post / post_x.py (cmd_hit_flash + _legacy_cmd_hit_flash_DEPRECATED 計9,111字 + parser + dispatch)。結果投稿は results (17:30) に一本化。**(2) GAS は死んでいると確定**: 直近の全 workflow_dispatch の triggering_actor を API で確認 → github-actions[bot] (watchdog/race_day_runner) と手動のみ、**GAS の定時発火 (7:30/12:30/20:00) はゼロ**。なのに yml の設計コメントは「GASからの直接呼び出し(メイン)/cron はフォールバック」のまま = 実態と逆。コメントを実態 (cron 主体 + watchdog 保険) に書き換え、scripts/gas_trigger.js を削除。**(3) 検証で潔白も2件確定** (誤った思い込みの訂正): 平日3スロットの題材は `get_todays_race(slot)` に重賞ローテーション実装済みで設計通り / cron 遅延は #56 の冗長 cron + concurrency で対処済み。**残るねじれ = モデルの odds 92% 依存** → ユーザー決定: **能力モデル (odds 非依存) を別途新設する value betting 路線** (現行モデルは印・表示用に維持、能力評価 vs 市場オッズの乖離 = 妙味検出。#26/#29 の教訓により backtest で edge の優位性を実証してから本番統合)。**教訓: 「ねじれの疑い」は必ず一次情報 (run 履歴の triggering_actor、ガード条件と発走時刻の突合) で検証してから手を入れる — 今回4候補中2件は調査で潔白と判明し、無駄な改修を回避できた。スロットを足す時は「その時刻に投稿条件が物理的に成立するか」(完走ガード × 発走時刻) まで確認する**。
- **#58 (2026-06-10) — 能力モデル (odds 非依存) を学習し value betting を walk-forward 実証 → 投資用途は見送り判定**: #57 の C-1 (model_rank が gain 92% odds 依存) への対応として、市場特徴量 (odds_log/popularity_norm) を除いた 50 特徴量で単勝勝率の「能力モデル」を新設 (`scripts/train_ability_model.py`、`fast_train.build_feature_table()` に共通化リファクタ、特徴量テーブルは /tmp に pickle キャッシュで再イテレーション 30分→数秒)。学習 ~2024 / 評価 2025-2026 (6.9万頭) の完全 out-of-sample。**結果**: (1) モデル自体は健全 — AUC 0.747、重要度は距離適性 11.9% / 騎手系 ~17% / 直近着順 7.7% と分散。(2) **素の edge (p_model/p_market) は逆指標** — 閾値を上げるほど ROI 悪化 (72→66%)。原因は favorite-longshot bias: 市場情報の無いモデルは確率が flat になり edge 上位が平均 100-150 倍の大穴に吸い寄せられ、大穴は構造的に過剰人気。(3) **オッズ帯制御で信号は実在** — edge≥1.5 × 3-10倍 ROI 90.1% / 10-30倍 86.0% (ベースライン 73.3% 比 +13-17pt、単日支配なし、両年安定)。(4) EV×帯 (v2-B) の最良も 88.4% (odds2-10×EV≥1.5, n=399) で **100% 超えの実用セグメントは出ず**。(5) stacked 2段 (v2-C, ロジスティック) の係数が決定的: **p_market 1.56 vs p_model 0.32 = 市場は能力モデルの ~5 倍の情報量**。stacked の ROI 177-457% は n=3-10 件で統計的に無意味。**判定: 投資用途は見送り** (「backtest で実証してから統合」の方針通りの健全な見送り。#31 の「6年 walk-forward で 100% 超え segment は極小2個」とも整合)。**残る活用価値: コンテンツ用途** — 現行モデルの予想は市場オッズの写しなので「AI が人気馬を本命にするだけ」になりがちだが、能力モデルは**オッズ非依存の本当の「AI独自評価」**として投稿の切り口 (sec_notable_horses の新 mode 等) に使える。models/model_ability_win.pkl + calibrator 保存済み・評価レポートは docs/analysis/ability_model_eval.json。**教訓: (1) 素の p_model/p_market 乖離は使えない — 必ず favorite-longshot bias を帯制限か stacking で殺す。(2) stacking の係数比は「自分のモデルが市場に対して持つ情報量」の正直な物差しになる。(3) 「edge に +13-17pt の優位がある」と「投資して勝てる (>100%)」の間には控除率 20% の壁があり、混同しない。(4) 負の結果も実証して記録する — 30分×2 の backtest が「やらない」判断を確定でき、本番を汚さずに済んだ**。
- **#59 (2026-06-10) — レース日の結果収集3系統を race_day_runner に一本化 (棚卸し A 項目)**: #57 棚卸しで判明した「土日 9-19 時に refresh_live (15分 cron) + race_day_runner (10分 loop) + collect_results (毎時 cron) が**ほぼ同一の collect/odds/payouts/export/push を3重並走**」を解消。これは push 競合 (#37/#52 で実害) と Pages ビルド連打の温床だった。**整理**: (1) **race_day_runner を唯一の日中収集ループに** — 機能は3者の完全 superset (collect/odds/payouts/preflight/sync/export/deploy + 投稿 safety-net dispatch)。primary cron を 8:50→**8:30 JST に前倒し**して旧 refresh_live の8時台更新を吸収 (バックアップ 8:50/9:00/9:30/10:00 + concurrency 直列化は維持)。(2) **refresh_live.yml 削除** (機能は runner の部分集合)。(3) **refresh_watchdog.yml を「Race Day Runner Watchdog」に転用** — 5分毎→15分毎、「runner の in_progress/queued run が無ければ再起動」のシンプルな生存監視に書き換え (旧: refresh_live の鮮度監視 + collect_results の夜間監視)。loop が途中で死んでも最大15分で自動復旧。(4) **collect_results.yml を夜間最終スイープに縮小** — 日中毎時 (10-17時, 19時) の cron を削除し 20:00/22:00 JST の2本のみ (runner 終了後の取りこぼし+遅出し配当回収)。**結果: 土日日中の DB push 主体が 3系統→1系統になり、`-X theirs` 自動解決で予測が巻き戻るリスクと Pages 連打が構造的に減る**。app.py / auto_post_x.yml の旧 workflow 言及コメントも実態に更新。**教訓: 「冗長配置。どちらかが動けばOK」は安心のつもりが push 競合という新しい故障モードを生む。冗長化は『起動の冗長化 (複数 cron + 生存監視) × 実行の一本化 (concurrency + 単一 owner)』の形にする — 同じ仕事を複数系統に同時にさせない**。
- **#60 (2026-06-10) — 死蔵コード 8 ファイル + best_params.pkl を削除 (棚卸し B 項目)**: #57 棚卸しで参照ゼロと判定した候補を、repo 全体 (py/yml/sh/md/json/html、skills/tests 含む) の再 grep で確証してから削除。**削除 (9 点)**: `quick_predict.py` / `batch_predict.py` / `post_scheduled.py` + `generate_weekly_tweets.py` (相互参照のみの孤島ペア) / `analyze_strategy.py` / `backtest_strategy.py` (scripts/historical_backtest.py に置換済、参照は docstring の系譜言及のみ) / `optuna_optimize.py` / `retry_pedigree.py` / `models/best_params.pkl` (optuna_optimize 専用だった)。**調査の結果「残す」と判定したもの**: `bets` テーブル (0行だが app.py の収支管理 UI が現役で read/write する**機能**であり死蔵ではない) / `tweet_fact_check.py`・`course_stats.py` (post_x.py が import する現役) / `docs/data/.post_history.json` (#56 で「lock が push されない事故」を最後に止めた実績のある第2防衛線) / scripts/ の研究スクリプト群 (CLAUDE.md の教訓エントリが参照する分析の再現手段)。検証: 削除後に全本番モジュール import OK。**教訓: 死蔵削除は「参照ゼロの確証 → 削除 → import 検証」の3点セット。0行のテーブル ≠ 死蔵 (未使用の機能と死んだコードは別物)。相互参照しか無いファイルペアは2つで1つの孤島なので同時に消す**。
- **#61 (2026-06-10) — 土曜本番前リハーサルで欠陥3件を事前検出・修正 (#56-60 大改修の通し検証)**: 「データ無しでリハーサルできる?」→ 4層方式で実施: 層1=cron 時系列の静的突合 / 層2=過去日 6/7 を /tmp サンドボックス DB で完全再生 (再predict 24/24 ✅ + 投稿 dry-run) / 層3=新 race_day_runner を end_hour 短縮で本番 GH Actions 実走 + watchdog 検知クエリ検証 / 層4=土曜当日の実機監視 (残)。**発見1 (層1): 土日朝 7:00 predict cron に冗長なし** — #56 は平日のみで、6/6 の「cron 完全スキップ → 朝404」事故クラスの穴が残っていた → 7:40 JST 冗長 cron 追加。**発見2 (層2): 結果投稿の単一脆弱点** — `_build_results_from_db`/`_build_results_from_json` は両方 `posted_at` (seal) 依存で、seal は再predict/push競合/DB復元で消える (6/7 で実際に消えていた) と**結果投稿が silent skip で全滅** → posted_slots (atomic lock) に post_predict 成功記録があれば `_select_target_races` で対象を再計算する fallback を両 builder に追加。seal 消失状態の 6/7 で完全スレッド生成を実証。**発見3 (層3): stale checkout による lock 喪失レース** — 実走テストで odds_flash の lock が origin に届かないことを観測。原因: actions/checkout の SHA は run **作成時点**で固定されるため、ほぼ同時に dispatch された run は concurrency で直列化されても「先行 run が push した lock」を見ずに古い DB を採用し、`pull --rebase -X theirs` (binary は local 優先) が先行 lock を上書きする → **Force git DB step を `git fetch + reset --hard origin/main` に強化** (auto_post_x / race_day_runner / collect_results の3箇所) し常に真の最新を起点に。**層3の合格項目**: runner 起動→loop→preflight→deploy→safety-net dispatch→end_hour 終了 ✅ / watchdog の in_progress 検知クエリが稼働中 runner を正しく検出 ✅ / 時間外ガード ✅ / lock の Deploy push (#56 fix) が origin 到達 ✅ / 安全 no-op (水曜=レース無しで X 投稿ゼロ) ✅。**教訓: 大改修後の初本番をぶっつけにしない — 「静的突合 → 過去日サンドボックス → 短縮実走」の3層リハーサルは安い保険で、今回それぞれの層が異なる欠陥を1つずつ見つけた。CI の checkout SHA は run 作成時点で凍結される — 『直列化したから最新が見える』は誤り。push される共有状態 (lock) を読む job は必ず fetch+reset で最新化してから読む**。
- **#62 (2026-06-10) — #52-61 大改修後の整合性監査: コードは仕様通り、ただし GH cron の3重同時不発を実測 (昼スロット復旧)**: ユーザー「システム的なところで問題はない? 仕様通りになっている?」→ system-integrity-audit 実行。**機械監査は全 pass** — caller-site / stale threshold / doc 整合 / workflow チェーン / cron↔dedup_cron↔determine の3表突合 (未マップ・幽霊マップなし) / `gh workflow run` 参照先存在 / dispatch mode↔post_x subcommand 整合 (TYPE=post_predict→`post_x.py predict` は設計通り)。**しかし live 監査で進行中の障害を発見**: 当日昼スロットが missing (期限 15:00 の 33 分前)。原因は **12:30 cron + 13:10 冗長 cron (#56) + */30 watchdog の3系統が同一時間帯に全部 GH スケジューラ不発** — watchdog の実発火は 11:14 JST の後 3 時間沈黙 (*/30 指定でも実測 1.5-2.5h 間隔)。手動 dispatch で 14:28 JST に投稿完了 (ガード窓内)。**結論: コード層の冗長化はこれ以上積んでも GH Actions スケジューラ自体の同時不発には勝てない — 外部 cron (#42, cron-job.org 等から workflow_dispatch API を叩く) が「あれば良い」から「必須」に格上げ**。必要なもの: GitHub PAT (workflow scope) + 外部 cron サービス登録 (アカウント作成はユーザー作業)。**教訓: 監査は「コードが仕様通りか」と「インフラが仕様通り動いているか」の2層で行う — 今回コードは満点でもインフラ層で進行中の SLO violation が見つかった。同一ベンダーの同一メカニズム (GH cron) を何重に重ねても相関故障で同時に死ぬ。冗長化は故障モードの独立したベンダー/メカニズムで**。
- **#63 (2026-06-10) — cron 依存からの脱却: weekday_runner 新設 + GAS 復活キット (故障モード独立の4層化)**: ユーザー「cronしか方法はないの?」→ #62 で実証した GH スケジューラの相関故障に対し、独立した時計を2層追加。**(1) `weekday_runner.yml` 新設**: 土日 race_day_runner で実績の「起動さえすれば自分の時計で定刻 dispatch」方式を平日に展開。AM (6:50/7:05/7:20 起動 → 13:30 まで loop、7:33 morning / 12:33 weekday) + PM (19:10/19:25/19:40 起動 → 21:00 まで、20:03 evening)。dispatch 前に posted_slots を fresh fetch して確認 (+3分 grace でネイティブ cron 優先、不発時のみ代打)。6/10 型の障害 (11:15-14:28 スケジューラ全滅) でも朝に起動済みの runner が昼を自前で投稿できる。**(2) GAS 復活キット**: `scripts/gas_trigger.js` 再設計 + `docs/GAS_SETUP.md` (10分手順)。**旧 GAS の死因を特定 — PAT がスクリプトに直書きされ git に commit → GitHub secret scanning が自動失効 → 401 で静かに死亡**。新版はトークンを GAS Script Properties に保存 (コード非含有)、失敗時は例外でトリガー画面に可視化、hit_flash トリガー廃止反映。**最終構成: ①GH cron (本体+冗長) / ②runner (起動後は自前時計) / ③GAS (Google の時計) / ④watchdog (バックストップ) — 全層が atomic lock を尊重するので何重発火でも投稿1回**。**教訓: 「外部 cron サービス契約」だけが代替ではない — (a) long-running loop は『スケジューラ依存を起動の1点に圧縮』できる、(b) 既に死んでいた仕組み (GAS) の死因を調べると設計は正しくて運用 (秘密管理) が殺していたことがある。トークンをコードに書いた時点でその自動化は時限死する — secret scanning は容赦なく失効させる**。

## 🛡 投稿前の Pre-flight Check (2026-05-24 導入)

各 X 投稿コマンド / 予測 cron / 結果反映 cron の前に以下を実行:
```bash
python3 scripts/preflight_check.py YYYYMMDD --auto-fix
```

これにより:
- races テーブルにレースが揃っているか確認 (不足なら fetch 自動再実行)
- 各レースに出走馬が登録されているか確認 (不足なら scrape_shutuba を再呼び出し)
- 予測キャッシュが揃っているか確認 (不足なら predict 自動実行)

返り値: 0=OK / 1=警告 / 2=致命的。GitHub Actions では `--auto-fix` でほぼ自動復旧。
